# 第七轮对抗循环 · A5 修复登记（迁移机械门禁盲区 + 000001 down 崩溃续跑）

HEAD = 7c6ac9d，修复日期 2026-08-14。核心文件 `migration/tests/down_order_gate.rs`、`migration/tests/bare_sql_gate.rs`、`migration/src/m20260814_000001_center_outbox_operation_ids.rs`、新建 `migration/tests/m20260814_000001_center_outbox_operation_ids.rs`。**已 commit**（2026-08-14，主 agent 统一提交为 `a0b2bc0`）。

| ID | 严重级 | 修复内容 | 证据 file:line | 测试 |
|---|---|---|---|---|
| W7-D-5-① | NOTE→修 | down_order_gate 盲区①：语句内 SQL 注释可藏 DROP。`drop_table_names` 先剥 SQL 注释（`--` 行注释 + `/* */` 块注释，与 bare_sql_gate 同款 `strip_sql_comments`，引号字面量保留边界一致）再按语句分词——`DROP /* reason */ TABLE parents` 不再让 `/*` 吃掉 DROP 序列。同款基础顺带闭掉对称盲区：`raw_alter_references` / `raw_create_table_references` 改为同一「剥注释→按 `;` 切分→逐语句分词」基础（注释同样不能藏/伪造 REFERENCES 与 CREATE TABLE 形状；当前全库无此形态，门禁全量扫描验证不破） | `strip_sql_comments` down_order_gate.rs:293；`split_sql_statements` :351；`statement_words` :419；`drop_table_names` :433；`raw_alter_references` :812；`raw_create_table_references` :951 | 新增 `gate_checks_sql_comments_cannot_hide_raw_drops`（:1585）：`/* */` 与 `--`（跨行）两种拼写下，父先子违规必报、子先父放行 |
| W7-D-5-② | NOTE→修 | down_order_gate 盲区②：`ALTER TABLE x RENAME TO y` 改活表引用不留新边。新增 `raw_renames` 识别该形态（y 非 `_rebuild`/`_new`/`_old` 归一化模式），`apply_renames` 把全局边表里指向 x 的 FK 边重定向为 y（y 已存在边则合并去重）；「改名即改引用」语义同时覆盖被改名表自己的出边。当前树 RENAME 全部为 `x_rebuild/x_new/x_old/... RENAME TO x` 重建尾——`_rebuild` 系边已被 `live_table_name` 归一化、`_new` 系 builder 暂存边恰由本重定向归位（全量门禁逐条核验新增约束如 `resource_snapshots → resources`、`resources → endpoints` 均被子先父的既有顺序满足，绿） | `is_staging_name` :847；`raw_renames` :867；`apply_renames` :912；接入全量聚合 `migration_down_order_violations` :1201 与 `synthetic_edges` :1241 | 新增 `gate_redirects_fk_edges_through_live_table_renames`（:1672）：重定向后边表不再含旧名、父先子必报、子先父放行、已有边合并去重、staging 名（`parents_rebuild`）不重定向 |
| W7-D-5-③ | NOTE→修 | down_order_gate 盲区③：引号标识符内分号被切开。`split_sql_statements` 引号感知（`'...'` 含 `''` 转义、`"..."`、`` `...` ``、`[...]`），`DROP TABLE "weird;name"` 不再记录虚构删除；`sql_identifier` 改为先切 `(` 列清单再裁引号，带列清单的引号名 `"weird;name"(id)` 解析正确 | `split_sql_statements` :351；`sql_identifier` :270（先 `split('(')` 后 trim） | 新增 `gate_keeps_quoted_identifiers_whole_across_statement_splitting`（:1740）：双引号/方括号/反引号名整体归位（`drop_table_names` 直断 + 端到端：`CREATE ... REFERENCES "weird;name"(id)` 出边、父先子必报） |
| W7-D-6 | NOTE→修 | bare_sql_gate 多语句原始串只在首词做 DDL 判定：`ALTER TABLE x ADD COLUMN y; DELETE FROM z` 首词 ALTER 通过、`;` 后 DML 静默放行。新增 `split_sql_statements`（与 down_order_gate 同款「剥注释后、引号感知」）+ `sql_statements`，`raw_statement_violations` 对整串原始首词判定（保注释首词必败的既有纪律）通过后逐 `;` 段独立过首词 DDL 与 `ddl_embedded_dml`——migration 门禁与 persistence PRAGMA 门禁共用 | `split_sql_statements` bare_sql_gate.rs:372；`sql_statements` :441；`raw_statement_violations` :759；`migration_violations` :793；`persistence_violations` :834 | 新增 `raw_statement_splitting_checks_every_semicolon_separated_segment`（:1010）：6 例含 DML 后段必报（含 PRAGMA 门禁例）、纯多语句 DDL 放行（含引号内 `;` 与注释内 `;`）、单语句行为不破、注释首词仍败、`DELETE FROM x` 仍败 |
| W7-D-7 | NOTE→修 | m20260814_000001 down 崩溃续跑无路：sea-orm-migration 2.0.1 对 SQLite 默认不包事务，`DROP INDEX` 与 `DROP COLUMN` 各自自动提交，两步间崩溃后重跑 down 报「no such index」永久失败。覆盖 `use_transaction` → `Some(true)`（up/down 一起原子；DROP INDEX / DROP COLUMN / ADD COLUMN / CREATE INDEX 均为 SQLite 事务内合法 DDL），与重建类迁移纪律对齐 | `migration/src/m20260814_000001_center_outbox_operation_ids.rs:50`（`use_transaction` 覆盖），:32-48（原子性文档） | 新建 `migration/tests/m20260814_000001_center_outbox_operation_ids.rs`：`operation_id_column_and_index_round_trip_with_data_preserved`——up 后列与索引存在（sqlite_master）、down 后消失且种子行保留（原始 COUNT）、up→down→up 往返幂等（模拟崩溃续跑重跑路径）、注册位置推导步数（后加迁移不漂移） |

## 门禁验证

- **fmt**：`rustfmt --edition 2024` 只格式化 A5 四个文件（并行 agent 在其他文件在改，未跑全仓 fmt）；格式化后三目标复跑全绿。
- **定向测试全绿**（指令指定命令，一次跑齐）：
  `cargo test -p rutilus-migration --test down_order_gate --test bare_sql_gate --test m20260814_000001_center_outbox_operation_ids`
  - down_order_gate：**16/16**（自检 13 → 16，新增 3）；
  - bare_sql_gate：**6/6**（自检 5 → 6，新增 1）；
  - m20260814_000001_center_outbox_operation_ids：1/1。
  - 全量门禁测试（`migration_down_drops_children_before_parents`、`migration_bare_sql_is_ddl_only`、`persistence_raw_sql_is_test_only_pragma`）在含并行 agent 未注册的新迁移文件（`m20260814_000002_audit_paging_index.rs`、`m20260814_000003_center_outbox_operation_lookup.rs`，递归扫描自动覆盖）的当前树上跑绿——RENAME 重定向与逐语句切分对现有 28+ 迁移零行为破坏。
- **clippy**：不适用（测试文件与指令要求一致，未跑）。
- **计数 pin 提示（主 agent 同步 CI pin 注释用）**：指令给的是「down_order_gate 13→14+、bare_sql_gate 8→9+」；实际登记时点 bare_sql_gate 自检为 **5** 个（非 8），现为 **6**；down_order_gate 13 无误，现为 **16**。CI pin 注释按真实计数同步（bare_sql_gate 5→6、down_order_gate 13→16），指令中的 8 未在代码里找到对应基数。

## 诚实未竟项

1. **门禁自身残余（如实登记，未扩）**：
   - `strip_sql_comments` 只保留单引号字面量：双引号标识符内的注释标记（`DROP TABLE "weird--name"`）仍被读作注释——bare_sql_gate 既有登记边界的同款，未扩。
   - 非首段以注释开头：`"; -- c\nALTER ..."` 剥注释后该段首词为 DDL 即放行，只有整串首位置保留原始首词严格规则（raw_statement_violations 文档已述）。
   - bare_sql_gate 既有登记残差（`AS (VALUES ...)` 裸 VALUES 系、`AS SELECT(1)`/`AS SELECT*FROM` 粘合拼写）维持原状；逐段扫描不改变这些边界。
   - `is_staging_name` 名单恰为指令点名的 `_rebuild`/`_new`/`_old`：其他后缀（如 audit 系的 `_previous`/`_vocabulary`/`_execute`/`_center`/`_auth`）的 RENAME 按活表改名处理（重定向暂存边到 `audit_events`，当前树无此类边、无行为差异）。
   - builder 暂存枚举边若**没有**对应 RENAME 语句仍保持暂存名不被任何 drop 匹配（`_rebuild` 系靠 `live_table_name`、`_new` 系靠本次重定向，均需 RENAME 或 raw CREATE 在文件里可见）。
2. **W7-D-7 未覆盖面**：down 两步之间崩溃本身无法经框架注入（`use_transaction` 原子性只能通过 up→down→up 往返间接验证）；`operation_id` 惰性回填是 repository 行为，迁移测试不可触发——两者均已登记、未测。
3. **并行 agent 干扰**（登记时点）：app/application/persistence/operation-engine/web/ui/scripts 有并行改动落盘，`migration/src/lib.rs` 未动（新迁移 000002/000003 未注册）。A5 三目标测试在其上跑绿；全仓 clippy/全量复跑待并行 agent 收尾后由主 agent 统一执行。
