# 第七轮对抗（wave-seven）A3 区修复登记

A3 区：存储 / 补偿队列 / 备份恢复族 6 项确认发现（W7-F-2、W7-C-4、W7-F-4、W7-D-1、W7-D-2、W7-S-3=W7-P-4）。基线 HEAD = 7c6ac9d；已由主 agent 统一提交（2026-08-14，`a0b2bc0`），迁移 000002 已注册。

| ID | 严重级 | 修复内容 | 证据 file:line | 测试 |
| --- | --- | --- | --- | --- |
| W7-F-2 | MEDIUM | restore 重跑路径不再覆盖残留标记：`restore_database_files` 写标记前先检查 `-restore-pending` 残留，存在即拒绝（新变体 `RestoreError::RestoreInterrupted`，错误信息指引操作员先 open 校验或人工处置），混合对永远不会被记成 pre 状态；open 校验逻辑不变 | persistence/src/backup_snapshot.rs:241-266（预检）、:667-674（变体）、:897-948（两代场景测试） | `a_rerun_restore_refuses_to_legitimize_a_mixed_pair_from_the_first_generation` |
| W7-C-4 | LOW | 停机路径调换顺序：先 join 后台补偿 drain 任务（stop 信号触发退出），再跑 bounded final drain；两处分支（server 自停 / stop 信号）同改。在飞事件失败重入队先落地，final drain 的 2s 预算覆盖全部剩余；join 仍在 store 关闭前完成 | app/src/standalone_runtime.rs:2463-2467、:2493-2499（调换后的顺序） | `a_shutdown_requeue_from_the_background_drain_reaches_the_final_drain`（写闸保持 append 在飞 → 停机 → 落表使 append 失败重入队 → final drain 预算耗尽 warn 断言） |
| W7-F-4 | LOW | RUTMK002 迁移失败降级为 warn（携带失败原因），继续用旧信封服务（旧信封同样 AEAD 认证，不降低解锁安全性；下次 open 再尝试迁移）；open 与 initialize 两路径对称修改；删除不再可达的 `MasterKeyMigration` 错误变体；测试注入 seam（`LEGACY_MIGRATION_INJECTION`，cfg(test) 编译出） | app/src/standalone_runtime.rs:1909-1935（open 降级）、:2900-2919（seam）、:2931-2937（注入失败）；app/src/initialization_runtime.rs:135-156（初始化对称降级） | `a_failed_legacy_envelope_migration_degrades_to_a_warned_open`（注入失败 → open 成功 + warn 捕获 + 信封字节不变） |
| W7-D-1 | LOW | 审计分页排序加决胜列 `id DESC`（`ORDER BY occurred_at DESC, event_sequence DESC, id DESC`），等值键上 offset 翻页全序稳定；`warm_audit_tail` 复用同一查询，自动对齐 | persistence/src/audit_repository.rs:144-152（排序） | `equal_key_rows_page_in_a_stable_total_order_by_event_id`（同 (occurred_at, sequence) 三行：单行页拼接 == 全量、两次查询同序、首页为最大 id） |
| W7-D-2 | LOW | drain 持久化失败后先做事件存在性检查（按 event id 查）：在库（已提交但报错）→ 仅按时间位镜像进内存尾 + ack，不重插、不入队、不丢弃；不在库 → 既有非重试丢弃 / 瞬时重试路径；新增 store 方法 `audit_event_exists` 与时间位镜像 `mirror_persisted_audit_event_to_tail`（镜像序与 warm 序一致；残余：尾部序严格度受追加序约束，注释如实说明） | app/src/standalone_runtime.rs:713-766（存在性分支）、:642-679（时间位镜像）；persistence/src/audit_repository.rs:164-182（存在性查询） | `a_committed_but_errored_append_is_mirrored_by_the_drain_not_dropped`（中间事件直写 store 不镜像 → drain 重发 → 镜像入尾、不入库两次、分叉消失）；`audit_event_exists_answers_by_primary_key` |
| W7-S-3 = W7-P-4 | LOW | 新迁移 `m20260814_000002_audit_paging_index`：`CREATE INDEX ix_audit_events_occurred_at_sequence ON audit_events (occurred_at DESC, event_sequence DESC, id DESC)`（三列与查询 ORDER BY 完全对齐含新决胜列，覆盖索引免全表扫描+外部排序）；down 对称 DROP INDEX；DDL-only 满足 bare_sql_gate | migration/src/m20260814_000002_audit_paging_index.rs（up/down） | migration/tests/audit_paging_index.rs：up 后 `PRAGMA index_list` 断言存在 + down 后消失 |

## 附带变更

- persistence/src/backup_snapshot.rs:174-183：新增 `SqliteStore::acquire_write_gate`（`OwnedSemaphorePermit`），暴露 §20.1「暂停写事务」原语——W7-C-4 停机测试用它把 append 确定性地保持在飞（app 侧无法触达私有 `write_gate` 字段）。

## 门禁结果

- `cargo test -p rutilus-persistence audit`：15 passed（含 2 枚新测试）。
- `cargo test -p rutilus-persistence restore`：5 passed（含 1 枚新测试；既有 :730/:771/:801 与 app 回滚测试不受影响——预检只加在 `restore_database_files` 入口，:771/:801 直调 `write_restore_marker` 不走预检）。
- `cargo test -p rutilus --lib`：161 passed / 0 failed（含 3 枚新测试）。
- `cargo test -p rutilus-migration`：除 audit_paging_index 外全绿；bare_sql_gate 6 passed、down_order_gate 16 passed（对新增迁移文件的静态扫描通过）。
- 自己文件 `cargo fmt` 与 `cargo clippy`（persistence、app lib/tests、migration lib/audit_paging_index）零警告零错误。

## 迁移注册要求（主 agent 接线，勿忘）

1. `migration/src/lib.rs` 顶部 `mod m20260814_000002_audit_paging_index;`（按日期排在 `m20260814_000001` 之后）。
2. `Migrator::migrations()` 的 vec **末尾**追加 `Box::new(m20260814_000002_audit_paging_index::Migration)`——必须排在所有会重建 `audit_events` 表的迁移（000007/000013/000014 等）之后，up(None) 后索引才存活；测试的 down(None) 断言也依赖注册位置靠后。
3. 接线后 `cargo test -p rutilus-migration --test audit_paging_index` 应由「索引不存在」转绿。

## 诚实未竟项

- `migration/tests/audit_paging_index.rs` 当前失败（`the paging covering index must exist after up`）——唯一原因：migration/src/lib.rs 尚未注册（本区禁止编辑，留给主 agent）；测试代码本身编译、clippy、fmt 全绿，注册即转绿。
- `cargo clippy -p rutilus-persistence --all-targets` 与 `-p rutilus --lib --tests` 在并行 agent 的 in-flight 文件上仍有报错：`operation-engine/src/operation_store.rs:177`（clippy::doc-markdown，A 区并行改动）、`migration/tests/down_order_gate.rs`（`&mut Vec` 应改 slice，A 区并行改动）。本区文件已用 `-A clippy::doc-markdown` 单独验证全绿，未代修并行区。
- W7-F-4 测试用注入 seam 而非真实 legacy 信封：security crate 的 RUTMK001 推导私有、app crate 无 argon2 依赖（Cargo.toml 不在本区可改范围），真实场景（只读目录 + 真 legacy 信封）在 app 侧不可构造；seam 为 cfg(test) 编译出，生产路径零分支成本。真实信封的迁移成功路径由 security crate 自有测试 `legacy_v1_envelope_unlocks_and_rewraps_to_the_current_format` 覆盖。
- W7-D-2 时间位镜像的序约束残余（注释如实记载）：尾部序的严格度受「warm 序 + 追加序」约束，事件按 occurred_at 插入；越界邻居下位置为缓存不变量内的最优。
- W7-C-4 测试为全链路停机构造（真实 RedfishGateway + 假 server + 写闸 + 落表），耗时约 2s+（final drain 预算）；与并行 agent 共享文件期间曾遇 exe 锁（LNK1104），重试后通过。
