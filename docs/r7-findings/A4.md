# 第七轮对抗循环 · A4 修复登记（安全 / 制品 / web / UI / 脚本族）

HEAD = 7c6ac9d，修复日期 2026-08-14。核心文件 `application/src/center/projection.rs`、`web/src/auth.rs`、`web/src/lib.rs`、`web/tests/write_path.rs`、`ui/src/lib.rs`、`ui/src/i18n.rs`、`scripts/assert-tests-ran.sh`。**已 commit**（2026-08-14，主 agent 统一提交为 `a0b2bc0`）。

| ID | 严重级 | 修复内容 | 证据 file:line | 测试 |
|---|---|---|---|---|
| W7-S-1 | HIGH | 中心侧制品传输对齐站点侧 2 GiB 封顶：`decode_manifest` 在 zero-byte 检查后对 `total_bytes > ARTIFACT_MAX_SIZE_BYTES` 直接拒绝，走既有 decode 失败的吸收惯例（absorb_frame：warn + 光标推进，行不创建、文件不落盘）——中心协议流无 HTTP 状态码，注释说明拒绝语义与协议一致；声明值被 cap 界住后，`consume_artifact_chunk` 的 `end <= size_bytes` 检查即把累计字节界在 cap 内（与站点侧 `append_chunk` 两层语义一致），无需改动 chunk 路径 | `application/src/center/projection.rs:1453`（拒绝 + S1 注释）；import `artifact_store::ARTIFACT_MAX_SIZE_BYTES` :64 | 新增 2 例（`application/src/center/projection.rs` tests）：`an_over_cap_manifest_is_absorbed_without_a_row`（2 GiB+1 → 吸收、零行、零文件）、`an_exactly_cap_manifest_is_accepted`（恰 2 GiB → 接受，行落库、size 恰为 cap）；chunk 累计超声明吸收由既有 `out_of_order_chunks_are_absorbed_without_touching_the_file`（:2590 起）兜底，未改 |
| W7-P-6 | MEDIUM | me_limiter 的 by_ip 桶挂全表剪枝：`reserve_ip` 在 `reserve_ip_key` 后补 `sweep_if_due(&self.by_ip, now)`——IP 桶 inserts 计数达 `BUCKET_PRUNE_THRESHOLD` 即触发清扫，与 reserve 双桶路径同机制；剪枝只回收条目全部出窗的桶，不改变任何限流判定语义。N3 注释（struct 文档）补 me_limiter 说明 | `web/src/auth.rs:1236`（P6 注释 + sweep 调用，:1230-1243）；N3 注释 :1082-1095 | 新增 `me_limiter_reserve_ip_prunes_expired_ip_buckets_to_a_bounded_size`（:4737）：注入 2×threshold 个不同 IP（短窗口 + `RATE_WINDOW` 滑窗），断言桶数回落至恰一个 working set、过期键消失、`inserts_since_prune` 归零——构造参照既有 `rate_limiter_prunes_expired_buckets_to_a_bounded_size` |
| W7-N-2 | NOTE→修 | offset 违规独立文案：`ParseAuditLimitError` 拆为 `ParseAuditError` 三变体（Limit / Offset / Parameter），handler 按类回各自文案——offset 超限/非数字 → 「audit offset must be between 0 and 10000000」；重复/未知参数保持独立分类并明确指向参数本身（「audit query accepts only the limit and offset parameters, each at most once」）；limit 违规文案不变 | `web/src/lib.rs:2150-2178`（handler 分类回执）；`enum ParseAuditError` :3499-3513；parse 函数 :3442-3483 | `bounds_the_audit_query_limit`（`web/tests/write_path.rs:1371`）扩断言：三类违规各自精确匹配新文案（limit=0 / offset=10000001 / page=2 / offset=1&offset=2），原 BAD_REQUEST 循环保留 |
| W7-L-3 | LOW | load-earlier 失败不再静默：`AuditPage` 加 `load_failed` 标志，失败路径 `Ready(page.with_failed_load())`——窗口原样保留（既有 E-11 契约），form-error 段显示新文案（区分「审计日志不可用」与「更早页加载失败」）；重试/加载更早成功（`with_older`）或刷新自动清除标志 | `ui/src/lib.rs:4479`（字段）、:4500（`with_failed_load`）、:14953-14956（失败回执）、:15010-15025（错误段按状态选文案）；`ui/src/i18n.rs:1250`（`error_audit_load_earlier` En/Zh 新键，完整性测试自动覆盖） | 新增 `a_failed_load_earlier_keeps_the_window_and_shows_the_error_until_the_retry_succeeds`（:21773）：失败 → 窗口事件卡仍 1、`is_failed()` 真；重试成功 → 追加页、标志清除 |
| W7-N-1 | NOTE→修 | Loading 态不再清空整窗：`AuditListState::Loading` 变 `Loading(Option<AuditPage>)`——翻页进入加载时携带已载页，`event_cards()`/`count_text()` 经 `page()` 统一取「Ready 或 Loading(Some)」的窗口，事件卡与计数在加载期间保持渲染；初始加载/刷新用 `Loading(None)`；has_more/offset 逻辑不变（`has_older_events` 仍在 Ready 页上判定） | `ui/src/lib.rs:4459-4474`（枚举 + 文档）、:4492-4520（`page()`/`is_failed`/计数/卡片）、:14950-14952（翻页保留窗口） | 新增 `loading_a_page_keeps_the_loaded_window_rendered`（:21732）：`Loading(Some(page))` 事件卡 1 张、计数「1 audit event」、`is_loading()` 真；`Loading(None)` 空窗口；既有 `audit_query_projection_renders_secret_free_event_cards` 按新变体更新 |
| W7-M-1 | LOW | assert-tests-ran.sh 支持全限定名消歧：名字校验从裸名 `[A-Za-z0-9_]+` 放宽为逐 `::` 段校验（每段 Rust 标识符 `[A-Za-z_][A-Za-z0-9_]*`，段剥离循环避免 IFS 把 `::` 拆成空段）；匹配保持后缀语义（全限定名既精确匹配也后缀匹配）；--help 用法行与头部注释同步，并注明「跨模块同名时须用全限定名」；ci.yml 的 21 个 pin 名全仓唯一，保持裸名不改 | `scripts/assert-tests-ran.sh:115-140`（校验循环）、:38-46（注释）、:27-30（用法） | cargo 桩测试（同 R6 A5 风格，临时桩不落库）：T5 全限定名匹配 exit 0、T5b 裸名后缀匹配 exit 0、T5c 缺席的全限定名 exit 1、T6a `a::1b` exit 1、T6b 尾空段 `a::` exit 1、T6c 首空段 `::a` exit 1、T6d 杂字符 `a-b` exit 1；计数下限回归：低于 pin exit 1、恰 pin exit 0；`bash -n` 通过 |

## 门禁验证

- **fmt**：`cargo fmt --check -p rutilus-application -p rutilus-web -p rutilus-ui` 干净。
- **定向测试全绿**（各自完整运行）：
  - `cargo test -p rutilus-application`：369 单元（含 2 新 S-1）+ 4 集成 + 0 doc，全绿；
  - `cargo test -p rutilus-web`：11 个 test binary 全 ok（120 单元含新 P-6；`bounds_the_audit_query_limit` 更新后通过）；
  - `cargo test -p rutilus-ui`（host）：144 全过（含 2 新 UI 测试），i18n En/Zh 完整性测试自动覆盖新键。
- **clippy**：`-p rutilus-ui --no-deps` 干净；`-p rutilus-web` / `-p rutilus-application` 的 clippy 复跑被并行 agent 的进行中改动阻塞（见未竟项 1），A4 代码在三次定向测试中均已编译并通过。
- **wasm 产物再生**：按 ci.yml 同一 remap 流程（`--remap-path-prefix=C:\Users\franck=.cargo-home --remap-path-prefix=C:\Users\franck\Documents\rutilus=.` 下 `cargo build --locked -p rutilus-ui --target wasm32-unknown-unknown --release` + `target/rutilus-tools/bin/wasm-bindgen` 0.2.126 `--target web --no-typescript --out-name rutilus_ui`）覆盖 `web/assets/rutilus_ui.js` / `rutilus_ui_bg.wasm`（wasm-bindgen 工具已在 `target/rutilus-tools/bin/`，与 CI 一致）；字节级验证：新文案 En「Could not load the older audit page; the loaded window is kept.」与 Zh「无法加载更早的审计页；已载窗口保持不变。」各 1 处嵌入 wasm。
- **脚本验证**：见 W7-M-1 行的桩测试与计数回归，全过。

## 诚实未竟项

1. **application/web 的 clippy 与全量复跑被并行 agent 阻塞**（登记时点）：`application/src/center_sync.rs`（W7-E-3，45 行新增，非 A4 文件）处于进行中编辑——先后出现 E0271（`Box::pin` 包已有 future）、E0596（`outbound_rx` 非 mutable）等编译错误；`operation-engine/src/operation_store.rs:177`（并行 agent 改动）有 `doc_markdown` clippy 错误。A4 的三次定向 `cargo test` 均在其改动落盘**前**跑绿；并行 agent 收尾后需复跑 web/application 的 clippy 与测试确认。
2. **wasm 与 CI ubuntu 产物的字节级收敛**只能由下一次 CI 的 normalize+diff 门禁最终确认；本机已按完全相同的 remap 流程再生且新文案入 wasm 已验证。
3. **W7-S-2（制品无删除 API / 总量配额）不在本区**：按指令未实现，主 agent 登记为已知限制。
4. ci.yml 的 21 个 pin 名维持裸名（全仓唯一，A4 计数核实：ci.yml 共 24 处列出、去重 21 名），跨模块同名场景出现时才需改全限定名（脚本注释已说明）。
