# R7 对抗循环 A1 区修复登记：中心派发 / outbox 族

- 区域：A1（中心派发、中心 outbox、回执协议族）
- HEAD：7c6ac9d（本区改动已由主 agent 统一提交，2026-08-14，`a0b2bc0`，34 文件 +4591/-360）
- 说明：代码注释为英文，本登记为中文。本区改动文件：`application/src/center/dispatch.rs`、`application/src/center_sync.rs`、`operation-engine/src/operation_store.rs`、`persistence/src/application_adapter.rs`、`persistence/src/center_outbox_repository.rs`、`persistence/src/operation_repository.rs`、`migration/src/m20260814_000003_center_outbox_operation_lookup.rs`（新建），以及本登记文件。未触碰 `migration/src/lib.rs`（迁移注册见文末）。

| ID | 严重级 | 修复内容 | 证据 file:line | 测试 |
|---|---|---|---|---|
| W7-E-1 | HIGH | `ReplyTarget::Succeeded.events()` 的 lead-in 缺 `RemoteTaskCompleted`：从 WaitingRemote 出发四事件全部 InvalidTransition 被 `let _` 吸收、记录永不推进，中心操作在 `waiting-remote` 后永久卡死（站点真实行为：WaitingRemote 时发 `progress{state:"waiting-remote"}`，Task 成功后发 `OperationCompleted{succeeded:true}`，center_sync.rs:1482-1495 的 `report_open_entry`）。修复：lead-in 在 `ExecutionAccepted` 之前插入 `RemoteTaskCompleted`（从 WaitingRemote 走 WaitingRemote→(RemoteTaskCompleted)→Verifying→(VerificationPassed)→Succeeded 的既有合法路径；从 Running/Queued/Validating 出发该事件无效被 `let _` 吸收、其余照旧——顺序在两种路径下均给出相同结果，且 Queued 治愈路径不受影响）。 | application/src/center/dispatch.rs:866-881（lead-in 加 RemoteTaskCompleted）；domain/src/operation.rs:306-316（既有合法路径，无需改动） | dispatch.rs:4292 `a_succeeded_report_closes_a_waiting_remote_record`（waiting-remote 记录 + succeeded 回执 → Succeeded，重发被吸收）；dispatch.rs:4364 `a_failed_report_closes_a_waiting_remote_record`（waiting-remote + failed 回执 → Failed 回归） |
| W7-E-3 | MEDIUM | R6-E-01 的 Unknown 防护被端点 re-home 旁路：`find_unknown_outcome` 的确认读只查请求站点自己的队列，re-home 后 offer 行在原站点队列、B 站无行 → 不拦截 → 铸新 id 派发 → 同一物理 BMC 双执行。修复：确认读改跨站点——新 trait 方法 `CenterOutbox::find_offer_by_operation_across_instances`（默认实现 = 按地址站点走既有 per-site 读，无实例枚举能力的 store 语义正确；生产仓库覆盖为真跨实例读），`SqliteStore::find_offer_by_operation_across_instances` 按 `operation_id` 列查任意实例队列（新单列索引 `ix_center_outbox_operation` 服务；miss 后沿用 NULL 存在性门 + NULL 列 payload 扫描的跨实例版惰性回填，见 W7-P-2）。**顺带修复**：`&Outbox` blanket 在 HEAD 缺 `find_offer_by_operation` 委托（center_sync.rs:222-267 核对 git show），生产运行时 `&SqliteStore` 路径一直走 trait 默认的全量 `list_offers` 扫描、R6-E-04 的定向读被静默遮蔽——本区同时补上两条委托，定向读（per-site 与跨实例）在运行时路径首次真正生效。 | persistence/src/center_outbox_repository.rs:542-619（跨实例读 + 回填）；application/src/center_sync.rs:231-249（trait 默认）、:291-305（blanket 委托两方法）；persistence/src/application_adapter.rs:552-560（覆盖）；application/src/center/dispatch.rs:674-717（find_unknown_outcome 用跨实例确认）；application/src/center/dispatch.rs:2069-2095（mock 跨实例实现）；migration/src/m20260814_000003_center_outbox_operation_lookup.rs（索引） | center_outbox_repository.rs:1754 `the_cross_instance_offer_lookup_confirms_an_offer_in_any_instances_queue`（A 有行、B 无行、跨实例读命中 A；per-site 读 B 不可见）；center_outbox_repository.rs:1802 `the_cross_instance_offer_lookup_backfills_pre_migration_null_rows`（NULL 行跨实例回填）；dispatch.rs:4419 `an_unknown_outcome_blocks_the_re_homed_endpoints_dispatch`（A 站 Unknown → re-home 到 B → 派发拒绝且返回同一 operation_id、无新 offer） |
| W7-F-1 = W7-E-2 | MEDIUM | ack 剪枝无 instance 隔离：`prune_acked_operation_history` 的 keeper 与 delete 只有 OperationId + State=="acked" + Id.ne(keeper)，而 sequence 每实例独立分配——re-home 同 id 双站形态下 B 的 ack 删掉 A 的 acked 证据行（R6-E-01/R6-E-03 的证据面）。修复：`ack_outbox_entry` 在 ack 后重新读出的行上同时取 `operation_id` 与 `instance_id`（调用处把本行实例 id 传进剪枝），keeper 按 (InstanceId, OperationId, State=="acked") 取最新、delete 加 InstanceId.eq(本行实例)。F1 定向读语义不破：每 (instance, operation) 的 newest acked 行保留，`resolve_candidate`/`offer_site_from_offer_facts` 的定向读仍见各实例自身最新行。 | persistence/src/center_outbox_repository.rs:401-414（ack 后取行 + 传实例）；:745-789（剪枝按实例过滤） | center_outbox_repository.rs:1582 `ack_pruning_is_scoped_to_the_acked_rows_instance`（A/B 双实例同 operation_id：B 三次 ack 后 A 的 acked 证据行仍在、B 只剩自己的 keeper）；既有单实例剪枝测试 `ack_prunes_the_older_acked_rows_of_the_same_operation` 不破 |
| W7-F-3 = W7-E-4 | MEDIUM | Unknown 扫描缺 target 维度：`find_unknown_outcome` 过滤只有 source+endpoint+command，同一 endpoint 上 target-A 终态 Unknown 会永久误伤 target-B 的派发。修复：确认读到 offer 行后按 offer 事实（payload 中的 target 字符串，与 find_undecided/resolve_candidate 同一事实源）与请求 target 比对，一致才拦截；不一致视为同一 endpoint/command 上的另一操作、放行新派发。 | application/src/center/dispatch.rs:702-711（target 比对） | dispatch.rs:4480 `an_unknown_outcome_on_one_target_does_not_block_another_targets_dispatch`（target-A 终态 Unknown：target-A 同 command 仍 409 且返回原 operation_id；target-B 同 command 派发放行、铸新 id） |
| W7-P-1 | HIGH | per-site 闸门内 5 次全局扫描：`find_undecided` 逐候选态 `list_operations(Some(state))` 全表列出并逐行 XChaCha20 解密，`find_unknown_outcome` 同病；闸门（R6-C-1 原子性）保留，扫描必须收窄。修复：新 repository 方法 `list_operations_for_endpoint(state, endpoint_id)`——先经 `operation_targets` 的 endpoint 过滤取 operation id 集（新建单列索引 `ix_operation_targets_endpoint`，原表仅 (operation_id, target_id) 复合主键、endpoint 无索引），再按 id 集取行，state 过滤为有界集上的残余谓词；多目标操作按"任一 target 命中"返回一次（去重）。`OperationStore` trait 加同名默认方法（list_operations + 内存 first-target 过滤，全 store 语义正确），`find_undecided` 与 `find_unknown_outcome` 的 5+1 次扫描全部换用。 | persistence/src/operation_repository.rs:644-702（repository 方法）；operation-engine/src/operation_store.rs:185-204（trait 默认）、:320-326（blanket 委托）；persistence/src/application_adapter.rs:258-266（覆盖）；application/src/center/dispatch.rs:462-476（find_undecided 换用）、:681-687（find_unknown_outcome 换用）；migration/src/m20260814_000003_center_outbox_operation_lookup.rs（endpoint 索引） | operation_repository.rs:1637 `list_operations_for_endpoint_filters_by_endpoint_in_acceptance_order`（B endpoint 的行不进入 A 的扫描；state 过滤仍在；多目标操作每 endpoint 扫描恰一次；空 endpoint 空扫描）；既有 dispatch 幂等测试全绿（mock 走 trait 默认、内存过滤与旧行为逐位一致） |
| W7-P-2 | MEDIUM | 定向读 miss 无条件全队列解密：`find_outbox_entry_by_operation` 索引 miss 后 `.all()` 拉该实例全部行逐行解密；「列已回填但行不存在」与「列未回填」不可区分，从未入队的 id 每次调用全扫。修复：回退前先做 `SELECT 1 WHERE instance_id=? AND operation_id IS NULL LIMIT 1` 存在性检查——SQLite 索引收录 NULL 键、`IS NULL` 走 (instance_id, operation_id) 索引（000001）范围扫，成本 log N 级（如实评估，非全表扫）；无 NULL 行直接返回 None，有 NULL 行才走回填扫描，且扫描本身收窄到 NULL 列行（列已置值的行不可能藏目标操作——索引查询已 miss）。跨实例读（W7-E-3）同构：全库 NULL 存在性检查 + 全库 NULL 列扫描。 | persistence/src/center_outbox_repository.rs:449-481（per-site 门 + NULL 列扫描收窄）、:556-580（跨实例门） | center_outbox_repository.rs:1656 `a_directed_read_miss_on_a_backfilled_queue_never_decrypts_anything`（回填完毕后种入损坏密文行（列已置值）：对不存在 id 的 miss 干净返回 None、损坏行未被解密（直接定向读该行仍报 Corrupt，证明种入行真实不可解密、干净 miss 即扫描未跑的证据）；既有 NULL 回填测试不破） |

## 迁移登记（主 agent 接线）

新建 `migration/src/m20260814_000003_center_outbox_operation_lookup.rs`（未写入 `migration/src/lib.rs`）：

- **`up`**：`ix_center_outbox_operation`（center_outbox 单列 operation_id，服务 W7-E-3 跨实例查询与 W7-P-2 的跨实例 NULL 门）；`ix_operation_targets_endpoint`（operation_targets 单列 endpoint_id，服务 W7-P-1 幂等扫描的 endpoint 先行过滤）。
- **`down`**：`DROP INDEX` 两个（与 000001 同款裸 SQL DDL，`bare_sql_gate` 通过）；无表级变更、无外键对，`down_order_gate` 不受约束。
- 注册要求：在 `migration/src/lib.rs` 的 `mod` 表与 `Migrator::migrations()` 末尾追加 `m20260814_000003_center_outbox_operation_lookup`（在 000001 之后；审计区并行 agent 已建 `m20260814_000002_audit_paging_index.rs`，注册顺序 000002→000003 即可）。主 agent 注册前，本区持久化测试不依赖该索引（索引为纯性能 DDL，查询正确性已由测试独立覆盖）。

## 门禁结果

- **fmt**：对 7 个本区文件逐一跑 `rustfmt --edition 2024` 通过；未跑全仓 `cargo fmt --all`（并行 agent 在改其他文件）。`application_adapter.rs` 曾被并行区 linter 重排过，本区格式化后内容核对无丢失。
- **clippy**（`cargo clippy -p rutilus-application -p rutilus-persistence -p rutilus-operation-engine --lib --tests -- -D warnings`）：全过（本区文档注释两处 `doc_markdown`（XChaCha20 缺反引号）已修）。
- **测试**（全部通过）：
  - `cargo test -p rutilus-application center::`：80 passed，0 failed（含 4 个新测试与全部既有 dispatch/tracking 回归）。
  - `cargo test -p rutilus-persistence --lib`：231 passed，0 failed（含 4 个新 outbox 测试与 endpoint 扫描测试）。
  - `cargo test -p rutilus-migration --test down_order_gate --test bare_sql_gate`：16 + 6 passed，0 failed（两个门禁对 000003 全过）。
  - `cargo check`（全仓）：通过（执行时刻并行区在途文件恰好一致）。
- **未 commit**（循环纪律）。

## 诚实未竟项

1. **全仓 `cargo test -p rutilus-migration` 有一个红测试**：`tests/audit_paging_index.rs` 的 `the_audit_paging_index_exists_after_up_and_is_gone_after_down` ——审计区并行 agent 已建 `m20260814_000002_audit_paging_index.rs` 与其测试，但其迁移尚未注册进 lib.rs（主 agent 接线），与 A1 无关。
2. **执行窗口内多次被并行 agent 在途改动打断**：`session.rs`（A2 区）与 `backup_snapshot.rs`（其他区）的在途编辑曾让 `rutilus-application`/`rutilus-persistence` 的 test 目标短暂无法编译（`mark_disconnected` 参数、`acquire_owned` 变更），等待其落地后复跑全绿；另 `persistence` 曾出现一次 1 failed（`230 passed; 1 failed`），复跑后消失，判定为该窗口内并行区在途测试状态所致，本区测试复跑稳定全绿。
3. **迁移前 NULL 行的跨站确认缝隙已按设计收口**：跨实例读对 NULL 列行走全库 NULL 列 payload 扫描 + 惰性回填（首访回填后转索引路径）；若存量队列存在大量遗留 NULL 内容行，该扫描成本为 NULL 行数（非全队列）——如实登记，未做进一步收紧（中心 outbox 只含 offer 行，000001 之后写入的行均有列值，缝隙仅限迁移前存量）。
4. **`list_operations_for_endpoint` 的 SQL 过滤是"任一 target 命中"**：与 `find_undecided`/`find_unknown_outcome` 的内存 first-target 过滤在**多目标操作**上语义可能不同（SQL 多返回、内存再滤掉）；中心派发候选恒为单目标 center 操作，两者一致。方法 doc 已如实写明，内存过滤保留为权威过滤器（纵深防御）。
5. **W7-E-3 的 trait 默认实现**：无实例枚举能力的 store（web 测试 mock、binding/session 测试 mock）继承默认 = 老 per-site 语义，语义正确但无跨站能力；生产 store（SqliteStore）与 dispatch 测试 mock 均覆盖为真跨实例语义。默认实现仅在未覆盖 store 上降级，已 doc 注明。
