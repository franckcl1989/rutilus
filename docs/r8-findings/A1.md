# R8 对抗循环 A1 区修复登记：中心派发 / outbox 族

- 区域：A1（中心派发、中心 outbox、回执协议族）
- HEAD：38dbd4b（已由主 agent 统一提交，2026-08-14，`6d5e90e`，18 文件 +1958/-142）
- 说明：代码注释为英文，本登记为中文。本区改动文件：`application/src/center/dispatch.rs`、`persistence/src/center_outbox_repository.rs`、`persistence/src/operation_repository.rs`，以及本登记文件。未触碰 `application/src/center_sync.rs`（未确需）、domain crate（规范化函数放 application，见 W8-F-2）。

| ID | 严重级 | 修复内容 | 证据 file:line | 测试 |
|---|---|---|---|---|
| W8-E-2 | HIGH | W7-E-3 的「未决路径」孪生漏洞：`find_undecided` 的 endpoint 作用域扫描跨站找到候选，但单候选修复读（旧 dispatch.rs:533 与 `resolve_candidate` 旧 :612-616）走 per-site `find_offer_by_operation`，offer 位于他站队列时返回 None → 判「从未入队」→ `deliver_retry` 同一 operation_id 向新站再入队 → 双执行。修复：两处定向读（`resolve_candidate` 的 fall-through 与单候选「从未入队」确认闸）全部换用跨实例读 `find_offer_by_operation_across_instances`；命中他站 offer =「已在飞/已入队」——单候选修复不重投、返回既有 operation id（幂等语义），多候选路径经同一 `resolve_candidate` 同样不铸新 id；仅当任何实例队列都没有该操作的行时才判定「从未入队」并走 stranded-offer 修复。顺带修复：旧 :533 的 per-site 确认闸在「跨站 offer 目标不同」场景会漏过并**复用旧 id 投递不同目标**（id 跨目标复用），换跨实例读后该场景正确「fresh start」铸新 id。TTL 过期语义不变：跨站过期 offer 与同站一致——退休 stale 行 + 同 id 重投（§17.5 键不变）。acked 行语义（R6-E-04/F1 的 keeper 可见性）不变：acked 行对跨实例读可见，同 id 返回/重投。 | application/src/center/dispatch.rs:638（resolve_candidate 跨实例读）、:547（单候选确认闸）、:564-581（多候选 fall-through 注释） | dispatch.rs:4576 `a_re_homed_undecided_dispatch_returns_the_existing_operation_without_re_delivery`（单候选：同 id、offers==1、operations==1）；dispatch.rs:4636 `a_re_homed_multi_candidate_dispatch_returns_the_existing_operation_without_re_delivery`（多候选变体）；dispatch.rs:4699 `a_re_homed_dispatch_with_an_expired_offer_re_delivers_under_the_same_id`（TTL 边界语义钉死）；dispatch.rs:4754 `a_re_homed_different_target_dispatch_starts_fresh_instead_of_reusing_the_id`（id 不复用） |
| W8-F-2 | MEDIUM | target 裸字符串比对：`find_unknown_outcome` 的 `fact.target == request.target_odata_id.as_str()` 与未决路径 `newest.target != ...` 无规范化——无需 re-home，同站对仍 Queued 的 X 用拼写变体（大小写/尾斜杠/%xx）重试即双执行。修复：新共享函数 `canonical_target_key`（dispatch.rs，application crate）：先百分号解码（无效转义保留原样；解码后按 UTF-8 语义处理，字节级解码防 %C3%A9 类多字节转义错配），再 ASCII 小写，再去全部尾斜杠；内部空白保留（解码后 `%20` 与字面空格收敛为同一键，RFC 3986 不允许其他空格拼写）。两处比对（:624 与 :727 一带）双方操作数都经同一函数；offer 写入侧（enqueue :751）**保持原样字符串**——线上格式与站点共享、不归一化，且写入侧归一化会使修复前遗留行在变体拼写下漏过闸门（向后兼容论证），比对时对存储侧同样做规范化即等价于「写入侧同一函数」。 | application/src/center/dispatch.rs:811-863（`canonical_target_key` + `hex_digit`）、:648-659（resolve_candidate 比对）、:739-747（find_unknown_outcome 比对） | dispatch.rs:4894 `spelling_variants_of_the_target_hit_the_undecided_idempotency_gate`（大小写+尾斜杠变体、%31 转义变体 → 命中未决闸门返回既有 id、无新 offer；真不同 target → fresh id）；dispatch.rs:4977 `spelling_variants_of_the_target_hit_the_unknown_outcome_refusal`（两种变体 → `UnknownOutcomePending` 返回既有 id） |
| W8-P-1 = W8-F-6 | MEDIUM（硬顶时 HIGH） | `list_operations_for_endpoint` 全历史扫描：无 state 过滤无 LIMIT，终态行永久累积；find_undecided 5 态循环 + find_unknown_outcome 每 dispatch 6 次全 id 集扫描 + 大 IN；bundled SQLite 3.51.3 变量上限 32766，超限报 "too many SQL variables" → 该 endpoint 派发永久失败。修复（核验员三向中选 ①+② 组合）：**state 过滤进 id 查询**——`operation_targets JOIN operations ON state=?`（JOIN 起点 operation_targets，`Relation::Operation`），state 作用域扫描的 id 集收窄为该状态在该 endpoint 的在飞操作数，与全历史解耦；**IN 分批**——新模块级常量 `IN_PARAMETER_CHUNK = 999`（远低于 32766），即使无 state 的全历史列举也超限不可能；分批读后按 (created_at, id) 全量排序恢复单一 acceptance order（与 `list_operations` 同序）。state 过滤保留在行查询上作残余谓词（纵深防御）。 | persistence/src/operation_repository.rs:27-38（IN_PARAMETER_CHUNK）、:656-723（id 查询 JOIN + 分批 + 全局排序） | operation_repository.rs:1797 `list_operations_for_endpoint_bounds_the_id_set_by_state_and_chunks_the_in_list`（1005 终态行 + 3 候选：state 扫描只回 2 个 Queued（含多目标操作去重一次）；state-less 扫描 1007 行跨 IN chunk 边界（1005 > 999 两批）全部返回且按 (created_at, id) 有序；endpoint B 隔离不破）；既有 `list_operations_for_endpoint_filters_by_endpoint_in_acceptance_order` 不破 |
| W8-E-4 | NOTE→修 | mock 跨实例读与生产语义分歧：旧 mock 升序取最旧行（sequence 升序 `.next()`），生产降序取最新（(created_at, id) desc）；mock corrupt 行 `.ok()` 跳过 → 确认读在测试中 fail-open，生产 Corrupt 报错 → 503 fail-closed。修复：mock 对齐生产——按 (created_at, id) 降序取最新行；payload 无法解码为 `Envelope` 的行返回 `Err(MockStoreError)`（fail-closed，测试内即生产 Corrupt 方向）；合法非 offer 行（message 为 None 或他类）正常跳过。 | application/src/center/dispatch.rs:2150-2202（mock 跨实例读） | dispatch.rs:5041 `the_mock_cross_instance_read_matches_production_semantics`（两次入队同 operation：返回较新行而非最旧；合法非 offer 行跳过；corrupt 行 Err） |
| W8-C-6 | NOTE→修 | backfill 注释「no caller of this read holds the gate」措辞歧义：语境中「the gate」指 write_gate、断言本身准确，但 dispatch 持 per-site 闸门（R6-C-1，app crate `dispatch_gates`）调用跨实例读属实。修复：两处注释改为「no caller of this read holds the *write gate*」+ 注记 per-site 闸门是另一把锁、同一任务不会在持有它时等待 write_gate。 | persistence/src/center_outbox_repository.rs:496-503（per-site 读回填注释）、:601-608（跨实例读回填注释） | 无（纯注释） |

## 门禁结果

- **fmt**：`rustfmt --edition 2024` 对本区 3 个改动文件逐一跑通过（`--check` 复验 clean）；未跑全仓 `cargo fmt --all`（并行 agent 在改其他文件）。
- **clippy**（`cargo clippy -p rutilus-application -p rutilus-persistence --lib --tests -- -D warnings`）：全过（本区新 doc 注释的 `doc_markdown`（SQLite 缺反引号）、`items_after_statements`（函数内 const）、`explicit_into_iter_loop` 均按 pedantic 修正）。
- **测试**（全部通过）：
  - `cargo test -p rutilus-application center::`：87 passed，0 failed（含 7 个新测试：4×W8-E-2 + 2×W8-F-2 + 1×W8-E-4；全部既有 dispatch/tracking 回归含 W7-E-3/W7-F-3 的 re-home 测试）。
  - `cargo test -p rutilus-persistence --lib`：232 passed，0 failed（含 1 个新 W8-P-1 测试；既有 231 全绿，含 W7-P-1 endpoint 扫描测试）。
  - domain crate 未动，`cargo test -p rutilus-domain` 未跑（无 domain 改动）。
- **未 commit**（循环纪律）。

## 诚实未竟项

1. **trait 默认 per-site 读（center_sync.rs:191-210）存在与 W8-E-4 同型的 fail-open 分歧**：`CenterOutbox::find_offer_by_operation` 的默认实现（web 测试 mock、binding/session 测试 mock 继承）对 corrupt payload 行 `.ok()` 跳过，而生产 per-site 读报 `Corrupt` fail-closed；排序方向（`max_by_key(sequence)`）与生产一致。核验员本轮仅确认跨实例 mock（dispatch.rs），trait 默认未在清单内；跨实例读与 per-site 默认读的调用侧一致（dispatch 全路径），如需收紧需同步审计所有继承默认实现的 store 测试，建议下一轮登记。
2. **center_sync.rs:1236（V5E-1 站点侧 offer 接受校验）的 `offer.target == odata_id || starts_with` 比对未规范化**：拼写变体不会造成双执行（变体 → 校验拒绝 → fail-closed 方向），但合法变体 offer 会被误拒为 `TargetStateChanged`；与本轮 target 规范化同族，未在本轮清单，登记待议。
3. **W8-P-1 的 JOIN 在 state=Some 时输出 id 集有界，但 SQL 扫描面仍为 endpoint 全历史 target 行**（JOIN 左表走 `ix_operation_targets_endpoint` 后逐行 join operations 滤 state）：成本与修复前 id 查询一致（endpoint 历史行数），只是 IN 参数与解密面收窄到该 state 的在飞集——核验员三向中选 ①+② 的已知取舍，未做反向 JOIN（operations 先行 + state 索引 `ix_operations_state`）优化，如实登记。
4. **`list_operations_for_endpoint(None, ...)` 的 id 集仍与全历史成正比**（无 state 无界语义使然，IN 分批保证不超限、行解密仍逐行）：生产调用恒为 `Some(state)`，None 路径仅测试与 trait 语义兜底。
5. **执行窗口内并行 agent 在途改动**：本区执行期间未遇到编译中断；最终全量测试与 clippy 均在提交态 HEAD=38dbd4b + 本区改动上通过。
