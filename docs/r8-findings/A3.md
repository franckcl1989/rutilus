# 第八轮对抗（wave-eight）A3 区修复登记

A3 区：迁移 / 门禁族 2 项确认发现（W8-D-1、W8-S-2 = W8-W-1 = W8-F-9 三透镜互证同根）。基线 HEAD = 38dbd4b，修复日期 2026-08-14。核心文件 `migration/src/m20260814_000003_center_outbox_operation_lookup.rs`、`migration/tests/bare_sql_gate.rs`、`migration/tests/down_order_gate.rs`、新建 `migration/tests/m20260814_000003_center_outbox_operation_lookup.rs`。**未 commit**（按指令由主 agent 统一提交）。

| ID | 严重级 | 修复内容 | 证据 file:line | 测试 |
| --- | --- | --- | --- | --- |
| W8-D-1 | MEDIUM | m20260814_000003 漏覆盖 `use_transaction`——W7-D-7 同款崩溃续跑缺陷复发：up 两条 `CREATE INDEX`、down 两条 `DROP INDEX` 在 SQLite 下各自自动提交（sea-orm-migration 2.0.1 `should_use_transaction`：Some(v)=>v、None=>backend==Postgres），两步间崩溃后半途重跑 up 报「index already exists」卡死（无 IF NOT EXISTS）、down 报「no such index」永久失败并阻塞整条回滚链（无 IF EXISTS）。全库 11 个多语句迁移全部覆盖、唯独 000003 漏网，且无测试钉住。修复：覆盖 `use_transaction` → `Some(true)`（与 000001 的 W7-D-7 修复同款），`up`/`down` 整体原子提交；注释同步补「Why the whole migration commits atomically」段落（CREATE INDEX / DROP INDEX 均为 SQLite 事务内合法 DDL，覆盖成本为零） | migration/src/m20260814_000003_center_outbox_operation_lookup.rs:36-49（原子性文档）、:55-57（`use_transaction` 覆盖） | 新建 migration/tests/m20260814_000003_center_outbox_operation_lookup.rs：`operation_lookup_indexes_round_trip_with_data_preserved`——up 后两索引存在（sqlite_master 探测）、down 后两索引消失、up→down→up 往返幂等（模拟崩溃续跑重跑）、种子信封行在往返中存活、步数由注册位置推导（后加迁移不漂移） |
| W8-S-2 = W8-W-1 = W8-F-9 | LOW | 两门禁 `strip_sql_comments` 只保护单引号字面量，引号标识符内注释标记被读成注释起点：`"a--b"`/`` `a--b` ``/`[a--b]` 落入 else 原样复制、其后的 `--`/`/*` 触发注释分支吞掉行尾——三构造零违规：①`CREATE TABLE "a--b" AS SELECT * FROM src`（SQLite 合法标识符、CTAS 真实执行）剥后剩 `CREATE TABLE "a` 无 AS 对 → 绿（**bare_sql 侧未登记的 fail-open 漏报方向**；r7 A5.md 曾误述为「既有登记边界的同款」，实为单引号字面量保留、非同款）；②`DROP TABLE "a--b"; DELETE FROM operations` 的 DML 尾段连同 `;` 被吞 → 单段 DROP 过 → 绿；③`/*` 无配对吞到串尾、方括号/反引号同理。修复：两门禁 stripper 同款补全四个引号态（`'` 含 `''` 转义、`"` 含 `""` 转义、反引号、`[...]` 至首个 `]`），引号态内不剥注释、态外正常剥、未闭合引号跑至串尾——与同文件 `split_sql_statements` 的引号态逐字对齐，「same strip-then-split shape」声称方才真正成立 | bare_sql_gate.rs `strip_sql_comments` :322-403（四引号态分支，文档 :302-320）；down_order_gate.rs `strip_sql_comments` :298-379（同款对齐，文档 :278-296）；down_order 侧 r7 登记的「双引号标识符内注释标记残留」条款随本次修复关闭 | bare_sql_gate 新增 `quoted_identifiers_cannot_hide_ctas_or_dml_from_the_stripper`：①双引号标识符内 `--` 的 CTAS 必报 ②同形态 `;` 后 DML 尾段必报 ③反引号/方括号标识符内 `--` 的合法表名不误报（含多语句串）④单引号字面量内 `--` 不误报（既有行为钉住）+ 三种引号态的 `""`/`` `` `` 双引号转义断言；down_order_gate 新增 `gate_keeps_quoted_identifiers_whole_against_comment_markers`：①`"weird--name"` 整体归位非虚构 `weird` ②`;` 后子表 drop 不再被吞（父先子必报）③方括号/反引号名整体归位 ④单引号字面量内 `--` 不伪造/不吞 drop + 端到端：`CREATE TABLE children ... REFERENCES "weird--name"(id)` 出边、父先子 drop 必报 |

## 门禁结果

- **fmt**：`cargo fmt -p rutilus-migration` 只格式化本区四个文件，零改动后复跑全绿。
- **定向测试全绿**（指令指定命令，一次跑齐）：
  `cargo test -p rutilus-migration --test down_order_gate --test bare_sql_gate --test m20260814_000003_center_outbox_operation_lookup`
  - bare_sql_gate：**7/7**（自检 6 → 7，新增 1）；
  - down_order_gate：**17/17**（自检 16 → 17，新增 1）；
  - m20260814_000003_center_outbox_operation_lookup：1/1。
  - 全量门禁测试（`migration_bare_sql_is_ddl_only`、`persistence_raw_sql_is_test_only_pragma`、`migration_down_drops_children_before_parents`）同轮跑绿——stripper 扩引号态与逐语句切分对现有 29 个迁移零行为破坏。
- **既有自检全绿不破**：`ddl_embedded_dml_is_flagged`、`strip_sql_comments_removes_comment_text_and_keeps_literals`、`raw_statement_splitting_checks_every_semicolon_separated_segment`、`gate_checks_sql_comments_cannot_hide_raw_drops`、`gate_keeps_quoted_identifiers_whole_across_statement_splitting` 等 22 个既有自检原样通过。
- **迁移全链健康**：`cargo test -p rutilus-migration --test m20260814_000001_center_outbox_operation_ids` 通过（000003 修复不扰动 000001 同款形态）；000003 注册位置未动（lib.rs 既有，本区不碰）。

## 诚实未竟项

1. **引号态内的其他形态如实登记（未扩）**：四引号态保护的是「引号态内的注释标记」这一层；引号态内其他文法形态仍是 stripper 的既有边界——引号标识符/字面量内的 `;` 由 `split_sql_statements` 的同一引号态处理（无行为变化），引号态内的 `AS SELECT` 等字面序列（如 `"a AS SELECT b"` 标识符）在词级扫描下仍读作形状（既有登记边界的同款扩展，无语句持此形态）。
2. **W8-D-1 崩溃注入不可测**：两步之间崩溃本身无法经 sea-orm-migration 框架注入（`use_transaction` 原子性只能通过 up→down→up 往返从一致 schema 间接验证，与 W7-D-7 同口径）；「半途重跑报 index already exists / no such index」的行为差异由原子提交覆盖消除，非直接断言。
3. **计数口径**：指令给出的计数基线「bare_sql 6→?、down_order 16→?」与本机实测一致（bare_sql 6 → 7、down_order 16 → 17，各 +1 枚新自检）；CI pin 注释如需同步，按此真实计数更新。
