# R8 对抗循环 A2 区修复登记：运行时族（Center/Standalone 停机与审计尾）

- 区域：A2（运行时族 3 项确认发现：Center 停机 drain 顺序、审计内存尾与 store 序分叉、Center accept 循环 connections 无界累积）
- HEAD：38dbd4b（已由主 agent 统一提交，2026-08-14，`6d5e90e`）
- 说明：代码注释为英文，本登记为中文。本区改动文件：`app/src/center_runtime.rs`、`app/src/standalone_runtime.rs`，以及本登记文件。`persistence/src/audit_repository.rs` 未动——尾排序修复按核验员建议全部落在查询侧，store 无需改动。

| ID | 严重级 | 修复内容 | 证据 file:line | 测试 |
|---|---|---|---|---|
| W8-C-1 = W8-F-1 | MEDIUM | Center 停机分支 final-drain/join 顺序与 W7-C-4 纪律相反：旧代码 `drain_audit_compensation_final` 先、`drain_compensation`（join drain 任务）后，且注释自称「the same two-branch discipline as the Edge postures」与实际相反。窗口：drain 任务 select 只在 pass 之间响应 stop；pass 内瞬时失败重入队；final drain 先跑空队列即返回；进程退出丢弃队列——在飞 requeue 绕过 final drain 预算整批丢弃（概率 0.1-0.2% 但非零）。修复：两个分支（server 自停分支与 stop 分支）均调序为 join 在前、bounded final drain 在后，注释改写为引用 W7-C-4 纪律并逐句说明颠倒即 bug。 | app/src/center_runtime.rs:895-913（server 自停分支调序）、:916-934（stop 分支调序） | center_runtime.rs:2167 `a_center_shutdown_requeue_from_the_background_drain_reaches_the_final_drain`（确定性构造：写闸钉住 drain 任务 pass 在飞、`wait_for_in_flight_drain_event` 等队列恰剩 second、DROP TABLE 使在飞 append 必失败、stop 后持闸 3s > final drain 2s 预算——固定序下 join 先等闸释放、两事件都进 final drain，断言预算耗尽警告 `remaining=2`；颠倒序下 final drain 先跑完 2s 预算时 first 仍在闸上在飞，警告 `remaining=1`。两序均确定性区分） |
| W8-D-2 = W8-C-2 = W8-F-3 | LOW | 审计内存尾与 store 权威序分叉三模式：①`mirror_audit_event_to_tail` 无条件 push_back（幂等只查 tail.back()），并发提交序与 occurred_at 序相反时尾为 [B,A]，console `tail.iter().rev()` 把旧事件 A 显示在 B 之上（drain 成功路径同走）；②`mirror_persisted_audit_event_to_tail` 时间位插入用严格 `>`，等 occurred_at 组内新事件恒插组末尾，与 store 的 (occurred_at, event_sequence, id) 规范序无关；③offset==0 页走尾、offset>0 页走 store，页面边界落在分叉区时跨页重复/漏事件/时序倒挂。修复（核验员建议方向，查询侧排序消除全部三模式）：新比较器 `canonical_audit_tail_order`（store `ORDER BY occurred_at DESC, event_sequence DESC, id DESC` 的升序镜像），`list_recent_events` 的 offset==0 路径先全尾排序再反转取 limit——与 offset>0 的 store 页逐事件同序，跨页边界一致；warm_audit_tail 重建的规范序尾排序幂等无害；mirror 插入行为不改（尾序对 console 视图不再相关）。尾有界 1024，排序代价有界。 | app/src/standalone_runtime.rs:587-604（`canonical_audit_tail_order`）、:503-528（`list_recent_events` 排序返回，:520 排序点） | standalone_runtime.rs:4720 `the_tail_query_serves_the_canonical_order_across_a_mirror_divergence`（旧 occurred_at 后插入 → 查询仍按规范序返回 [newer, older]）；standalone_runtime.rs:4755 `the_tail_query_matches_the_store_within_a_shared_occurred_at_group`（等 occurred_at 三事件组：尾视图与 store 第一页逐事件全等，含 sequence 与 id tiebreak；组内 seq-2 事件恒居首） |
| W8-P-5 | LOW | Center accept 循环 connections Vec 运行期只 push（旧 center_runtime.rs:1039-1046）、仅停机 join（:1076-1083）；100 site × 30s 重连 × 24h ≈ 28.8 万条目 ≈ 15-20MB，W7-C-2 代际守卫合法化快速重连反而放大 churn。修复：抽出 `reap_finished_connections`（`JoinHandle::is_finished()` 逐项回收），accept 循环每次接受后回收——Vec 恒有界于「在飞连接数 + 1」。回收时对已完成的 handle await（已完成任务输出已存储，poll 立即 Ready，不悬挂 accept 循环），JoinError（panic/取消）按停机 join 同款日志纪律带 site id 记录——观测等价保留，无静默丢弃；停机 join 只处理仍在飞连接，不重复记日志。 | app/src/center_runtime.rs:1115-1141（`reap_finished_connections`）、:1073-1076（accept 后回收调用）、:1013-1032（循环文档注明有界性） | center_runtime.rs:2265 `the_accept_loop_reaping_keeps_the_connection_tracking_bounded`（finished + aborted + running 三任务：回收后仅 running 幸存；aborted 的 JoinError 经 CaptureSubscriber 断言带 site id 记录——「回收不丢观测」钉死） |

## 门禁结果

- **fmt**：`cargo fmt -p rutilus --check` 通过（改动文件为 app crate 内两文件；未跑全仓 fmt，并行 agent 在改其他文件）。
- **clippy**（`cargo clippy -p rutilus --tests`）：本区两文件 0 发现（过程中两处自身 pedantic 告警——`empty_line_after_doc_comments` 与 `duration_suboptimal_units`——已修正）。并行 agent 在途改动曾使 `persistence/src/operation_repository.rs` 未闭合分隔符致编译失败、以及两处 clippy 错误（`doc_markdown`、`items_after_statements`），最终快照下全仓 clippy 干净；执行期间受阻如实登记（见未竟项 1）。
- **测试**：
  - `cargo test -p rutilus --lib` 全量：**165 passed，0 failed**（最终代码态复跑）。
  - 新增 4 个测试全绿：W8-C-1 ×1（确定性 `remaining=2` 断言，单跑 5.3s）、W8-P-5 ×1、W8-D-2 ×2。
  - 既有回归不破：尾/镜像族 6 个（`restart_backfills_the_audit_tail_from_persisted_history`、`a_broken_tail_mirror_never_loses_the_persisted_event`、`a_committed_but_errored_append_is_mirrored_by_the_drain_not_dropped`、`a_corrupt_persisted_row_warms_the_tail_into_the_failed_state`、`a_failed_warm_up_marks_the_tail_and_the_query_reports_it`、`the_compensation_drain_persists_and_mirrors_a_queued_event`）、W7-C-4 停机测试、W7-C-3 center 补偿测试——全部通过。
- **未 commit**（循环纪律）。

## 诚实未竟项

1. **并行 agent 在途改动造成的执行期受阻**：执行期间 `persistence/src/operation_repository.rs` 一度处于未闭合分隔符的半改状态（A1 区并行 agent），`cargo test` 编译失败无法复跑；另有两处该文件的 clippy pedantic 错误。已等待其落盘后复跑，最终全量测试与 clippy 在结算树（HEAD=38dbd4b + 各区改动）上通过。未触碰 persistence 文件。
2. **W8-P-5 的循环级行为测试缺位**：reap 逻辑以确定性单测覆盖（finished/aborted/running 三态、JoinError 观测等价）；「多次注册-断开循环后 connections 长度回落」的完整循环级断言需要真实 mTLS site 客户端走 center 协议监听器（测试套件现无该客户端设施），未做。Vec 有界性论证（每次 accept 后回收 → 恒 ≤ 在飞 + 1）在代码注释与登记中写明，循环级观察留待后续迭代或站点运行时侧测试。
3. **W8-D-2 模式 ③ 的窗口覆盖语义不变**：查询侧排序使 offset==0 与 offset>0 视图逐事件同序，跨页边界不再重复/漏事件/倒挂；但 offset==0 页的数据窗口仍是尾缓存（最近 1024 事件，含 W7-D-2 中段插入时窗口可能略旧于 store 最新集）——该窗口语义是尾缓存设计本身，非本轮分叉问题，未改。
4. **`capture_warn_diagnostics` 加 `.with_ansi(false)`**：为字段断言（`remaining=2`）去掉 ANSI 转义；既有仅断言消息子串的测试不受影响（全量回归证实）。该改动随测试基建导出（pub(crate)）一并登记。
