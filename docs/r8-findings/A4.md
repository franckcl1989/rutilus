# 第八轮对抗循环 · A4 修复登记（文档 / UI / 文案族）

HEAD = 38dbd4b，修复日期 2026-08-14。核心文件 `ui/src/lib.rs`、`application/src/center/projection.rs`（仅注释）、`persistence/src/backup_snapshot.rs`（错误文案 + 模块文档）、`.github/workflows/ci.yml`（仅注释）、`docs/release-readiness.md`、`docs/milestone-status.md`、`docs/known-limitations.md`、`docs/security-review.md`、`docs/operations-manual.md`、`docs/user-manual.md`。**已 commit**（2026-08-14，主 agent 统一提交为 `6d5e90e`）。

| ID | 严重级 | 修复内容 | 证据 file:line | 测试/验证 |
|---|---|---|---|---|
| W8-W-5 | NOTE→修 | 审计错误文案 match 的 `Loading(_) => unavailable_audit` 陷阱（is_failed 守卫恒 false 挡住时不可达，守卫一旦对齐即显示错误文案）：内联 match 抽为 `AuditListState::error_hint_text()` 纯方法，`Loading(Some(page)) if page.load_failed`（翻页重试在途、携带 L3 失败窗口，N1 语义）与 `Ready` 同款按携带页 `load_failed` 标志选 `error_audit_load_earlier`——重试在途时窗口保留、失败未解除，提示必须继续点名「更早页加载失败」而非「审计日志暂时不可用」；`Loading(None)`/无失败页保持 unavailable 文案；注释说明守卫与文案选法的同构关系与潜在性（is_failed 对 Loading 恒 false，该 arm 今日潜在、为未来守卫对齐保持诚实） | `ui/src/lib.rs:4576-4588`（方法 + 文档）、`ui/src/lib.rs:15046-15047`（视图调用点，替换内联 match）、测试钉 const `AUDIT_LOAD_EARLIER_ERROR_HINT` `ui/src/lib.rs:4602` | 新增 `a_retry_in_flight_after_a_failed_load_earlier_keeps_the_earlier_failure_hint`（`ui/src/lib.rs:21857`）：`Loading(Some(with_failed_load))` → 文案 = `error_audit_load_earlier` 英文钉、`is_failed()` 仍假（守卫语义钉住）、`Loading(None)` → unavailable 文案。i18n 无新键（复用 `i18n.rs:1247, 1250`） |
| W8-E-1 | LOW | decode_manifest 超 cap 注释如实化：原「at-least-once outbox keeps the frame until the site learns of the skip」与实现相反（absorb_frame = warn + advance_cursor，帧被 ack、制品从中心视图消失、站点无任何反馈）；重写为真实行为描述（吸收一次、游标推进、无行残留、站点无从得知、人工检查中心视图才能发现），并注明协议级拒绝反馈为未来工作（known-limitations 登记由主 agent 统一执行，注释内引用 §九）。同时核验 :913-918 与 :970-980 附近——无同款误导注释，无需改动 | `application/src/center/projection.rs:1440-1452`（S1 注释块重写）；`absorb_frame` 真实行为 `:1141-1154` | 行为零改动（注释-only）；`cargo check -p rutilus-application` 通过 |
| W8-D-3 = W8-F-10 | LOW | 混合对恢复死锁的文案与文档修复（判定逻辑不动）：①`RestoreError::RestoreInterrupted` 错误文案重写——删除「resolve the marker manually」（删标记即洗白入口），改为明示洗白后果（混合对状态下删标记 → 重跑把混合对记为 pre → open 判定 untouched 接受）与正确恢复步骤（先用保留的 pre-restore 副本构成完整对 → 启动实例一次让 open 校验清除标记 → 重跑 restore）；②模块文档 :225-232 按分情形描述——完整对（快照对或 pre 对）由 open 接受并清标记，混合对被 open 拒绝、须先构完整对再 open，删标记只会在下次重跑时重开 W7-F-2 洗白洞；③operations-manual 新增 §6.5「恢复中断（混合对）处置」（识别症状 / 正确恢复步骤 / 删标记后果警告） | `persistence/src/backup_snapshot.rs:663-671`（错误文案）、`:225-236`（模块文档）；`docs/operations-manual.md:206-225`（§6.5） | `cargo test -p rutilus-persistence backup_snapshot` 9 个定向测试全过（含 `a_rerun_restore_refuses_to_legitimize_a_mixed_pair_from_the_first_generation` `:897`）；`verify_restore_marker` 判定逻辑未动 |
| W8-E-3 = W8-S-1 | NOTE | 终态 Unknown 永久冻结的文档出路（机制不改，主 agent 已按 W7-E-5 先例划出威胁模型）：user-manual 新增 §10.1「Unknown 终态与 409 unknown_outcome_pending 对账」——触发场景（(站点,端点,命令,目标) 键上存在终态 Unknown 时派发被拒，HTTP 409 + 稳定码 `unknown_outcome_pending`，拒绝本身写审计结局 Refused）、operation_id 含义（中心操作视图可查；re-home 后确认读跨实例，命中 offer 可能来自原绑定站点队列，**可能是他站操作**）、对账三步（定位操作 → 人工核验 BMC 实际状态 → 效果已发生则 409 是正确的防重行为、未发生见已知边界）、已知边界（无解锁/清除 API：终态吸收回执、站点侧跳过终态、无 reaper、409 响应无结构化 operation_id，同键重派发在控制台不可行，需人工介入），并交叉引用 known-limitations §九第七波块 W7-E-6 / W7-E-7b 登记 | `docs/user-manual.md:422-441`（§10.1，§十既有「操作状态为 Unknown」行 :418 衔接） | 文档-only；交叉引用两行在 `docs/known-limitations.md:444-445`（第七波块，未动） |
| W8-W-2 | LOW | M-2 重锚遗漏「UI 本地化」行的 lib.rs 锚点：`LanguageSelector` `lib.rs:11647`→`:11725`、wasm 薄封装 `lib.rs:11614-11636`→`:11692-11724`、`start()` `lib.rs:11668`→`:11746`，计数「ui 141 测试全过」→「144」（实测 `cargo test -p rutilus-ui -- --list` = 144）；来源列范围 `11614-11668`→`11692-11746` 同步。历史点-时登记（milestone-status.md §7.5 c4dd335 行、known-limitations.md:178）保留不动 | `docs/release-readiness.md:300`；`docs/milestone-status.md:558`；`docs/known-limitations.md:132`（锚点 + 计数）；实测锚点 `ui/src/lib.rs:11692-11746`（`stored_lang_code` `:11692`、`LanguageSelector` `:11725`、`start()` `:11746`） | 文档-only；三行行号在第八波登记合入后可能被主 agent 的登记推偏，重锚为主 agent 收尾职责（见未竟项 2） |
| W8-W-3 | LOW | security-review.md:195 两处未重锚：Secret leak gate 锚点 `ci.yml:285`→`:307-309`（步骤实际位置）、milestone-status 锚点 `:459`→`:513`（原指到迭代二 blockquote，「Secret 泄漏检查」行实际在 :513，release-readiness 已用 513 坐实原锚错误） | `docs/security-review.md:195`；实测 `ci.yml:307-309`、`docs/milestone-status.md:513` | 文档-only |
| W8-W-4 | NOTE | A5 登记的 CI pin 注释同步未执行：Migration Test 门禁注释「38 基线 + 12 down_order_gate」按**改动时刻实测**重写——`cargo test --locked -p rutilus-migration -- --list` = 67（down_order_gate 17 + bare_sql_gate 7 + 其他迁移测试 43，2026-08-14 A4 实测），注明 W8-W-4 重同步与「wave-eight 门禁自检仍在落盘中，集成时须重测」；pin 50 与 floor 语义不动 | `.github/workflows/ci.yml:603-613`（注释，仅注释） | 注释-only；计数以集成复跑为准（A3 并行增测中，见未竟项 1） |

## 门禁验证

- **fmt**：`cargo fmt --check -p rutilus-ui` 干净；`-p rutilus-application`/`-p rutilus-persistence` 的 fmt diff 全部位于并行 agent 文件（`application/src/center/dispatch.rs`、`persistence/src/operation_repository.rs`），A4 的四文件零 diff。
- **clippy**：`cargo clippy --locked -p rutilus-ui --all-targets` 零 error（首轮新 match 触发 pedantic `match_same_arms`，已合并同体 arm 修复）。
- **测试**：`cargo test --locked -p rutilus-ui` 145 全过（含新 W8-W-5 测试）；`cargo test --locked -p rutilus-persistence backup_snapshot` 9 全过；`cargo check --locked -p rutilus-application` 通过。
- **wasm 产物再生**：按 ci.yml 同一 remap 流程（`--remap-path-prefix=C:\Users\franck=.cargo-home --remap-path-prefix=C:\Users\franck\Documents\rutilus=.` 下 `cargo build --locked -p rutilus-ui --target wasm32-unknown-unknown --release` + `target/rutilus-tools/bin/wasm-bindgen` 0.2.126 `--target web --no-typescript --out-name rutilus_ui`）覆盖 `web/assets/rutilus_ui_bg.wasm`（js 无新串不变）；二次再生字节级一致（`diff` 干净）；strings 验证 En「Could not load the older audit page; the loaded window is kept.」与「The audit log is temporarily unavailable.」均在 wasm 内。

## 诚实未竟项

1. **migration 计数以集成复跑为准**：A3 区并行给 bare_sql_gate / down_order_gate 加自检（改动落盘进行中），A4 实测 67 = 17 down_order_gate + 7 bare_sql_gate + 43 其他（A5 登记时点为 65 = 16/6/43）；ci.yml 注释已按「截至 A4 改动时刻的实测」书写并在注释内注明需重测，若 A3 增测后计数再变，主 agent 集成时须再同步。
2. **docs 三处现行状态行号会被第八波登记推偏**：release-readiness.md:300、milestone-status.md:558、known-limitations.md:132 为 A4 重锚后的当前行号，主 agent 的第八波登记（并行编辑同一批 docs 文件）合入后行号可能漂移，重锚为主 agent 收尾职责。
3. **W8-E-1 的 known-limitations 登记由主 agent 统一执行**：projection.rs 注释仅引用（「registered in known-limitations §九」），未在 docs 侧新增登记行。
4. **W8-D-3 无代码级防线**（按指令）：人工删标记的洗白后果仅以错误文案 + 运维指引 + 模块文档警示，`verify_restore_marker` 判定逻辑按指令未动。
5. **W8-E-3 机制不改**（按指令）：Unknown 终态冻结、无解锁 API、无 reaper 均为登记过的已知限制（known-limitations §九 W7-E-6 等），本区仅补用户侧文档出路。
6. **wasm 与 CI ubuntu 产物的字节级收敛**只能由下一次 CI 的 normalize+diff 门禁最终确认（同 r7 A4 先例）；本机已按完全相同的 remap 流程再生且二次生成字节一致。
