# 第九轮对抗（wave-nine）A3 区修复登记

A3 区：迁移事务覆盖族 1 项确认发现（W9-D-1，覆盖 10 个文件）。基线 HEAD = a63c810，修复日期 2026-08-14。核心文件：`migration/src/m20260805_000001_initial_storage.rs`、`m20260805_000003_resource_snapshots.rs`、`m20260805_000004_audit_events.rs`、`m20260805_000005_operations.rs`、`m20260805_000006_remote_tasks.rs`、`m20260805_000007_artifacts.rs`、`m20260805_000008_events.rs`、`m20260805_000009_telemetry.rs`、`m20260805_000010_groups_tags.rs`、`m20260805_000011_batch_operations.rs`，新建 `migration/tests/m20260805_initial_transaction_round_trip.rs`。**已 commit**（2026-08-14，主 agent 统一提交为 `ba110ce`）。

| ID | 严重级 | 修复内容 | 证据 file:line | 测试 |
| --- | --- | --- | --- | --- |
| W9-D-1 | LOW | 10 个最老的建表迁移（000001/000003/000004/000005/000006/000007/000008/000009/000010/000011）无 `use_transaction` 覆盖也无手动事务——sea-orm-migration 2.0.1 `should_use_transaction`：`Some(v)=>v`、`None=>backend==Postgres`（vendored 源码 migrator/exec.rs:200-205），SQLite 下每条 DDL 自动提交：多语句迁移在两句之间崩溃则半途重跑 `up` 报「table already exists」、`down` 报「no such table」永久卡死。这是 W7-D-7（m20260814_000001 修复）与 W8-D-1（m20260814_000003 修复）缺陷的第三次复发面；r8 A3.md 登记的全库覆盖口径（「11 个多语句迁移全部覆盖」）与任何实际枚举对不上，且无「建表迁移免事务」的有意决策登记。修复：10 个迁移逐一补 `use_transaction → Some(true)`（与 W8-D-1 同款），并逐文件补「Why the whole migration commits atomically」文档段（说明语句构成、崩溃续跑卡死机理、SQLite 事务内 DDL/DML 合法性与零成本——000001 额外点名 down 的 `active_version_id` NULL-out UPDATE）。修复后覆盖集：**22 个 override + 4 个手动事务（m20260807_000001/000003/000006、m20260812_000002）= 26 个多语句迁移全覆盖**；剩余 4 个无覆盖的迁移（000002 单 CREATE TABLE、m20260807_000002 单 ALTER、m20260812_000001 单 CREATE TABLE、m20260814_000002 单 CREATE INDEX）均为单语句，无崩溃缝隙，不在缺陷族内 | 各文件 `use_transaction` 覆盖与文档段：m20260805_000001:3-21（文档）、:26（覆盖）；000003:3-21、:23；000004:3-21、:23；000005:17-34、:37；000006:20-37、:40；000007:16-33、:36；000008:29-46、:49；000009:26-43、:46；000010:31-52、:57；000011:26-45、:48 | 新建 migration/tests/m20260805_initial_transaction_round_trip.rs 3 枚：`initial_storage_transaction_round_trip`（000001——up(Some(1)) 建 6 表 + uq_credentials_name 索引、种子 6 行含 credential→version 双向外键引用、down(Some(1)) 全表消失、re-up 后表复归且 0 行——down 的 NULL-out UPDATE 在有真实引用行时成功即证整迁移原子完成）；`groups_tags_transaction_round_trip`（000010——4 表 + uq_groups_name 往返，种子 group/member/tag/binding）；`batch_operations_transaction_round_trip`（000011——batch_operations + `operations.batch_id` 列 + ix_operations_batch_id 往返；**唯一真数据保留断言**：down 后 operations 种子行存活（COUNT=1）、batch_id 列消失，re-up 后行仍在、batch 表空）。步数均由注册位置推导（`migrations_through`），后加迁移不漂移 |

## 门禁结果

- **fmt**：`cargo fmt -p rutilus-migration` 只对本区 11 个文件执行；复跑 `--check` 本区文件零 diff。
- **定向测试全绿**：
  `cargo test -p rutilus-migration --test m20260805_initial_transaction_round_trip` → **3/3**。
- **全量 `cargo test -p rutilus-migration` 全绿（74 枚集成测试，含新 3 枚）**：全部 29 个测试二进制 0 失败；其中两门禁对 10 个修改文件的静态扫描必须绿——`migration_bare_sql_is_ddl_only`（bare_sql_gate 8/8）、`migration_down_drops_children_before_parents`（down_order_gate 19/19）均通过（本轮新增语句仅 `use_transaction` override 与注释，未引入任何 `execute_unprepared`，000011 既有 raw ALTER 过检不变）；既有全链测试（initial_storage.rs 的 `migrations_preserve_storage_invariants` 跑 up(None)→up(None)→down(None) 全链）与全部既有单迁移测试（operations.rs、remote_tasks.rs、events.rs、telemetry.rs、groups_tags.rs、batch_operations.rs 等）原样通过。

## 诚实未竟项

1. **抽样测试理由（3/10）**：000001——最大（up 11 语句：6 建表 + 5 索引；down 7 语句：6 删表 + 1 UPDATE），是唯一 down 内混合 DDL+DML 的迁移，且最可能先于任何数据存在时被重跑；000010——语句最宽的表组形态（4 表 + 3 索引 + 双 FK 链）；000011——10 个里唯一含 raw `ALTER TABLE ... ADD COLUMN`（内联 REFERENCES）且唯一 down 保留数据的迁移，因此也是唯一有真数据保留断言的。其余 7 个（000003..000009）为同构单文件形态（1-2 建表 + 1-3 索引、down 全删表），由同款 `use_transaction` 覆盖 + 既有全链测试（每个注册迁移的 up/down 端到端跑）覆盖。
2. **崩溃注入不可测**：与 W8-D-1 同口径——两步之间崩溃无法经 sea-orm-migration 框架注入，原子性只能通过 up→down→up 往返从一致 schema 间接验证；「半途重跑报 table already exists / no such table」的行为差异由原子提交覆盖消除，非直接断言。
3. **指令计数勘误**：指令称 000010「9 语句」，实测 up = 7 语句（4 建表 + 3 索引）、down = 4 删表；文档按实测写入（000010 文档段:31-52）。
4. **机械检查建议（防第四次复发，本区不实现，登记留待下一轮/主 agent 决策）**：新增静态自检（可作为 down_order_gate/bare_sql_gate 的扩展或独立自检）：扫描 `migration/src` 每个迁移文件，凡 `up`/`down` 语句总数 > 1 者必须带 `use_transaction` override 或手动 `.begin()` 事务，否则报错。单语句迁移（000002、m20260807_000002、m20260812_000001、m20260814_000002 共 4 个）豁免。建议在 A3.md 登记此口径为门禁正式语义，替代 r8 的「全库多语句迁移全部覆盖」式人工登记。
5. **并发工作区**：本仓库为共享工作区，`migration/tests/bare_sql_gate.rs`、`down_order_gate.rs` 及 application/ui/docs 等文件存在其他区 agent（W9-T-3 等）的在途改动；本区未触碰任何区外文件，全量测试在含其在途改动的共享工作区跑绿（其新增 W9-T-3 自检 2 枚编译并随两门禁通过）。其新代码处存在 fmt 漂移（down_order_gate.rs 约 2475 行），属其工作区，本区未代格式化。
6. **测试计数**：全套 74 枚集成测试（此前基线 + 本区 3 枚）；bare_sql_gate 8/8（其中 1 枚为并发区新增）、down_order_gate 19/19（其中 2 枚为并发区新增）——门禁计数含并发区在途新增，非本区成果。
