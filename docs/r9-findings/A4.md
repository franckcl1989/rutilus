# 第九轮对抗循环 · A4 修复登记（文档 / CI / 测试覆盖族）

HEAD = a63c810，修复日期 2026-08-14。核心文件 `.github/workflows/ci.yml`（仅注释）、`docs/milestone-status.md`、`docs/known-limitations.md`、`docs/release-readiness.md`、`docs/operations-manual.md`、`docs/user-manual.md`、`ui/src/lib.rs`（仅测试）、`migration/tests/bare_sql_gate.rs`（仅测试）、`migration/tests/down_order_gate.rs`（仅测试）。**已 commit**（2026-08-14，主 agent 统一提交为 `ba110ce`；docs 行号漂移的最终同步见 a63c810→本轮重锚）。

| ID | 严重级 | 修复内容 | 证据 file:line | 测试/验证 |
|---|---|---|---|---|
| W9-CI-1 | LOW | ci.yml Migration 门禁 ran-assertion 注释按「改动时刻实测」重同步：67→**71**（19 down_order_gate + 8 bare_sql_gate + 44 其他迁移测试；W9-T-3 增测后实测），"43 other"→44；pin 50 与 floor 语义不动；缓冲句保留并注明「wave-nine 门禁自检仍在落盘中，集成时须重测」（A3 区往返测试文件落地后计数仍会再变） | `.github/workflows/ci.yml:610-622`（注释，仅注释）；实测 `cargo test --locked -p rutilus-migration -- --list` = 71（19+8+44） | 注释-only；计数以集成复跑为准 |
| W9-CI-2 | NOTE（由 PARTIAL 定案） | workspace pin 1913 注释双失效重写：「余量侵蚀是缺陷」不成立（floor 语义注释自明、防删测试由 `--expect-tests` 承担），剩余实质 = 点-时表述过时——注释更新为改动后实测 2016（`cargo test --locked --workspace -- --list`；wave-seven/eight 已推至 2013），并注明 pin 1913 不动的原因：floor 语义下 pin 为点-时下界、不随计数增长、防删测试由 `--expect-tests` 名单承担（引用下方 Migration 步骤注释的同款 floor 说明） | `.github/workflows/ci.yml:155-165`（注释，仅注释）；实测 workspace `--list` = 2016 | 注释-only；pin 1913 与 `--expect-tests` 名单未动 |
| W9-D-1（W9-W 编号） | LOW | UI 本地化三处现行状态行锚点被 W8-W-5 的 error_hint_text 插入推偏 +26，重锚为实测值：`stored_lang_code` `:11692`→`:11718`、`LanguageSelector` `:11725`→`:11751`、`start()` `:11746`→`:11772`（wasm 薄封装区 `11692-11724`→`11718-11746`、来源列 `11692-11746`→`11718-11772`）。历史点-时登记（`r8-findings/A4.md` 本行旧值、`known-limitations.md:482`）保留不动 | `docs/milestone-status.md:578`；`docs/known-limitations.md:132`；`docs/release-readiness.md:313`；实测锚点 `ui/src/lib.rs:11718`/`:11751`/`:11772` | 文档-only；逐符号 grep 实测后重锚 |
| W9-D-2（W9-W 编号） | LOW | operations-manual §七迁移计数 28→**30**（补 `m20260814_000002_audit_paging_index`、`m20260814_000003_center_outbox_operation_lookup` 两个迁移名，明细改写为 `m20260814_*` 3），并重锚 R6-D-2 的 `Migrator::migrations().len()` 派生位置——旧锚 `backup_snapshot.rs:943-944, 983-985` 已被 wave-seven/eight 推偏，实测新锚 `:489`（supported 派生）、`:1043`（pending）、`:1074-1075, 1115-1116`（applied/supported 两测试）；milestone-status 同款失效锚三处同步（wave-six 段「现」行、§7.2 备份恢复演练行、E4 行；历史登记行 249-250/756/778-779 的「原」锚点叙述保留） | `docs/operations-manual.md:239`；`docs/milestone-status.md:251`、`:537`、`:581`（E4 行）；实测 `persistence/src/backup_snapshot.rs:489, 1043, 1074-1075, 1115-1116` | 文档-only；逐符号 grep 实测后重锚 |
| W9-D-3 | NOTE | user-manual §10.1 对账步骤 1 补站点作用域限定：site-scoped 角色（Operator/Viewer，`role_assignments.site_id` 有分配）仅能查看其分配站点的操作——命中的是他站操作时，需全局 Administrator 或对应站点作用域账号查看；注明 `view_scope_allows` 语义（Administrator 全站；Operator/Viewer 无作用域分配则全站、有分配则仅分配站）与 `center_operations` 行为（列表对无权他站操作静默过滤，带 `site_id` 查询无权站时返回 403） | `docs/user-manual.md:430`（§10.1 步骤 1）；语义实测 `web/src/auth.rs:298-309`（`view_scope_allows`）、`web/src/lib.rs:4171-4195`（403 分支 + 列表静默过滤） | 文档-only；语义按代码逐分支核验 |
| W9-T-2 | NOTE→修 | `error_hint_text` 两个可见态缺断言：既有测试（已构造 `Ready(page.with_failed_load())`）补两处 `assert_eq!`——`Ready(load_failed)` → `AUDIT_LOAD_EARLIER_ERROR_HINT`（复用既有 const，不重复硬编码）、`Failed` → `Lang::En.strings().unavailable_audit`（与既有 `Loading(None)` 断言引用同一文案源）；`Loading(Some(..))` 在途态由 W8-W-5 既有测试钉住，本补丁补齐可见双态 | `ui/src/lib.rs:21826-21846`（新断言，位于既有 `a_failed_load_earlier_keeps_the_window_and_shows_the_error_until_the_retry_succeeds`）；文案源 const `:4602`、`Loading(None)` 先例 `:21894-21897` | `cargo test --locked -p rutilus-ui` 145 全过（144→145，+1 断言测试计数不变） |
| W9-T-3 | NOTE→修 | 两门禁未闭合引号形态（`DROP TABLE "unterminated`）按「跑至串尾」语义补自检：bare_sql_gate 钉剥离（未闭合引号内 `--`/`/*` 是标识符文本、输出原样）与切分（未闭合引号内 `;` 非终止符、整串 1 条语句）；down_order_gate 钉切分 + 剥离 + 端到端（`drop_table_names` 把未闭合片段吞成单个 drop 名、后续 `DROP TABLE` 属同一语句）。**双份实现互检**：include! 方案实测不可行（对方文件 `//!` crate 头注释在 mod 内触发 E0753、且会重复运行对方全部测试），改选**静态函数体逐字符对比**——读对方源码、大括号平衡提取 `strip_sql_comments`/`split_sql_statements` 两函数体，断言与己方逐字符一致（文本相同 ⇒ 行为相同，比样本输出对比更强：样本未覆盖的输入也不会漂移），两函数体当前实测逐字符一致 | `migration/tests/bare_sql_gate.rs:1046-1075`（新测试 `unterminated_quotes_run_to_the_end_of_the_string`）；`migration/tests/down_order_gate.rs:1837-1876`（`gate_runs_unterminated_quotes_to_the_end_of_the_string`）、`:2459-2520`（互检 `the_sister_gate_stripper_bodies_are_identical` + `fn_body` 提取器） | `cargo test --locked -p rutilus-migration --test down_order_gate --test bare_sql_gate` 全绿（down 17→19、bare 7→8；互检经 awk 预演后落盘，实测通过） |

## 门禁验证

- **fmt**：`cargo fmt --check -p rutilus-ui -- ui/src/lib.rs` 干净；`-p rutilus-migration` 的 fmt diff 全部位于 A3 区并行新文件（`migration/tests/m20260805_initial_transaction_round_trip.rs`），A4 的两个门禁文件零 diff。
- **测试**：`cargo test --locked -p rutilus-ui` 145 全过（含 W9-T-2 新断言）；`cargo test --locked -p rutilus-migration --test down_order_gate --test bare_sql_gate` 全绿（19 + 8）。
- **实测计数**（A4 改动后，供主 agent 集成同步）：migration `--list` = **71**（19 down_order_gate + 8 bare_sql_gate + 44 其他）；workspace `--list` = **2016**；ui 145。

## 诚实未竟项

1. **migration / workspace 计数以集成复跑为准**：A3 区并行给 10 个建表迁移补事务并加往返测试文件（`m20260805_initial_transaction_round_trip.rs` 已落盘），migration 计数还会再变；ci.yml 两处注释（Migration 71、workspace 2016）均按「截至 A4 改动时刻实测」书写并已在注释内注明「re-measure at integration」，最终同步为主 agent 集成职责。
2. **docs 行号漂移收尾为主 agent 职责**：A4 改动的 docs 行（milestone-status:251/537/578/581、known-limitations:132、release-readiness:313、operations-manual:239、user-manual:430）在第九波登记合入后可能被主 agent 的登记推偏；本区只改既定行与锚点、未新增波次登记内容，重锚收尾归主 agent。
3. **互检采用静态函数体对比而非运行时 include**：include! 编译方案因对方文件 `//!` crate 头注释（E0753）与测试重复运行不可行，已如实改选更严格的逐字符函数体对比（两函数体当前实测一致）；未来任一方在函数体内改动（含注释）会触发互检测试失败——这是防漂移的预期行为，更新时须同步两份实现。
4. **W9-D-3 为文档-only**：site-scoped 查看边界由既有 `view_scope_allows` 门禁与 `center_operations` 过滤实现（`web/src/auth.rs:298-309`、`web/src/lib.rs:4171-4195`），本区未改任何判定逻辑。
