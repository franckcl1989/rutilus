# R6 对抗循环 A1 区修复登记：中心派发 / 绑定 / 协议

- 区域：A1（中心派发、站点绑定、§15.6 回执协议）
- HEAD：e85560a，修复后未提交
- 说明：代码注释为英文；条目中的"待 A6 映射"指 `web/src/lib.rs` 的 `CenterOperationRefusal` / `center_dispatch_refusal_response` 与 `api` 的 `CenterOperationRefusalCode` 需要新增的变体，本区域未触碰 web 错误映射。

| ID | 严重级 | 修复内容 | 证据 file:line | 测试 |
|---|---|---|---|---|
| R6-C-1 | HIGH | dispatch 幂等扫描与创建无原子性：`StandaloneState` 新增 per-site `tokio::sync::Mutex` 闸门（`dispatch_gates`），`CenterServices::dispatch_center_operation` 将 绑定检查→find_undecided→create_operation→enqueue_offer 整体纳入同一临界区；锁仅在 dispatch 内持有，无重入路径。 | app/src/standalone_runtime.rs:307-313（字段）；app/src/center_runtime.rs:472-486（取闸门+持锁）；app/src/center_runtime.rs:487-515（临界区） | app/src/center_runtime.rs:1566 `two_concurrent_identical_dispatches_create_one_operation`（两个并发相同派发 → 同一 operation id、恰 1 条非终态操作） |
| R6-E-01 | HIGH | Unknown 后重发：`CenterDispatchError` 新增 `UnknownOutcomePending { operation_id }`；`dispatch` 在 find_undecided 无候选后检查同 (site, endpoint, command) 的终态 Unknown 操作（站点经 outbox 定向行确认，防 re-home 误伤），存在则返回类型化拒绝码且不铸新 id。 | application/src/center/dispatch.rs:283-300（新变体）；application/src/center/dispatch.rs:389-397（调用点）；application/src/center/dispatch.rs:655-697 `find_unknown_outcome` | application/src/center/dispatch.rs:3978 `a_retry_after_an_unknown_outcome_is_refused_with_the_existing_operation_id`（拒绝码稳定、无新 operation/offer） |
| R6-C-2 | MEDIUM | 重绑 TOCTOU：`bind_with_code` 事务内追加复查——同 `site_cert_fingerprint` 且 `state='bound'` 且 id≠本行 → 回滚并返回新错误变体 `FingerprintAlreadyBound`；revoked 行不阻塞同站重绑。 | persistence/src/center_binding_repository.rs:194-223（复查）；persistence/src/center_binding_repository.rs:423-426（新错误变体） | persistence/src/center_binding_repository.rs:683 `bind_refuses_a_fingerprint_already_bound_to_another_registration`（先 Bound 后同指纹 bind 失败、行保持 pending、revoke 后重绑成功） |
| R6-E-02 | MEDIUM | 吊销站可派发：`dispatch_center_operation` 入口（临界区内）检查 binding 状态，非 Bound → 类型化拒绝（web 层照常审计 Refused 结局）；`list_center_endpoints` 过滤掉绑定不在效的站点的投影端点（每站一次 binding 查询，无 site 关联的坏行同样不展示）。 | app/src/center_runtime.rs:489-503（绑定闸）；app/src/center_runtime.rs:343-381（端点列表过滤） | app/src/center_runtime.rs:1606 `dispatch_to_a_revoked_site_is_refused_and_the_revocation_ends_the_session`（revoke 后派发被拒、无 operation 生成） |
| R6-E-03 | MEDIUM | re-home 回执被拒：`offer_site` 在投影存在但与回执站点不一致时，先走 `offer_site_from_offer_facts`（现为按 operation_id 定向查询）确认，回执站队列持 offer 即授信；两侧都不同意才拒绝。 | application/src/center/dispatch.rs:1103-1120（offer_site 仲裁）；application/src/center/dispatch.rs:1122-1140（定向事实回退） | application/src/center/dispatch.rs:4052 `a_reply_from_the_original_site_credits_after_the_endpoint_was_re_homed`（投影指向他站 + 合法 offer 事实 → 授信） |
| R6-E-04 | MEDIUM | outbox 无剪枝 + O(N) 解密：新迁移 `m20260814_000001_center_outbox_operation_ids`（`center_outbox` 加 plaintext `operation_id` 列 + `ix_center_outbox_instance_operation` 索引；down 删索引/删列，无表级 DROP，down_order_gate 通过）；插入时从同一条 payload 解析写入（center_outbox_repository.rs:93-100/132-140）；`ack_outbox_entry` 剪枝同 operation 的其他 acked 行（保留最新一条，:380-393/590-620）；`resolve_candidate` 与 `offer_site_from_offer_facts` 回退路径改为定向查询（dispatch.rs:602-615、1122-1140）；迁移前 NULL 行在首次定向读时按 payload 惰性回填（:420-477，F1 双执行防护跨升级成立）。 | migration/src/m20260814_000001_center_outbox_operation_ids.rs（全文件）；persistence/src/center_outbox_repository.rs:331-477；application/src/center_sync.rs:180-215（trait 默认实现） | persistence/src/center_outbox_repository.rs:1246/1307/1374（插入写列+定向读 newest、ack 剪枝保留最新、迁移前行回填）；`cargo test -p rutilus-migration --test down_order_gate --test bare_sql_gate` 通过 |
| R6-E-06 | LOW | 进度折叠：`reply_target` 解析 `OperationProgress.state`（wire 上站点真实状态码），`waiting-remote` → 新 `ReplyTarget::WaitingRemote`（事件含 `RemoteTaskStarted` lead-in），其余状态维持 Running。 | application/src/center/dispatch.rs:788-796（新目标）；application/src/center/dispatch.rs:821-833（事件）；application/src/center/dispatch.rs:1344-1358（解析） | application/src/center/dispatch.rs:4102 `a_waiting_remote_progress_reply_tracks_the_remote_task_state`；application/src/center/dispatch.rs:4297 `reply_target_parses_the_wire_progress_state` |
| R6-E-08 | LOW | 未知操作回执不留痕：`on_reply` 对不可解析 id / 找不到操作的回复，先写吸收型回执行（received 相位、envelope 原样入库、operation_id 原样——不可解析时用原始字符串的确定性 UUIDv5 派生键）再 warn+Ok。 | application/src/center/dispatch.rs:950-995（两条吸收路径）；application/src/center/dispatch.rs:1178-1198（record_absorbed_reply）；application/src/center/dispatch.rs:1271-1302（raw id 提取与派生键） | application/src/center/dispatch.rs:4202 `absorbed_replies_for_unknown_operations_leave_a_durable_receipt`（含同原始 id 重发只落一行） |
| R6-C-6 | NOTE | 退役竞态：`CenterSessionRegistry` 增加每站 disconnect 信号（`disconnect_watch`/`disconnect`，Notify 语义保证与 mark_connected 竞态不丢唤醒）；`revoke_center_binding` 撤销成功后踢除注册表并触发信号；连接任务在共享停止信号之外 select 该信号关闭连接。 | application/src/center/session.rs:305-311（信号字段）；application/src/center/session.rs:377-410（disconnect_watch/disconnect）；app/src/center_runtime.rs:449-456（撤销踢除）；app/src/center_runtime.rs:1037-1076（连接任务观察信号） | application/src/center/session.rs:1340 `disconnecting_a_site_fires_its_disconnect_signal_and_removes_it`；application/src/center/session.rs:1373 `disconnect_of_a_site_that_is_not_online_is_a_no_op`；app/src/center_runtime.rs:1606（revoke 后 is_online=false 且信号触发） |

## 待 A6 agent 映射的新变体名单（本区域未碰 web/src/lib.rs）

1. **`CenterOperationRefusal::SiteBindingRevoked`**（web/src/lib.rs）——当前 `center_runtime.rs:516-518` 用 `NotAuthorized`（403）作临时映射（带 `TODO(R6-A6)` 注释）。
2. **`CenterOperationRefusal::UnknownOutcomePending { operation_id }`**（web/src/lib.rs）——当前 `center_runtime.rs:543-546` 用 `UnknownEndpoint { endpoint_id }`（422）作临时映射（带 `TODO(R6-A6)` 注释）。
3. 相应 `CenterOperationRefusalCode` 新值（api/src/lib.rs，如 `site_binding_revoked` / `unknown_outcome_pending`）与 `center_dispatch_refusal_response`（web/src/lib.rs）新分支；`CenterOperationRefusalCode` 已有 `#[serde(other)] Unknown` 兜底，新增值对旧 console 无线兼容问题。

## 门禁结果

- `cargo fmt --all --check`：通过。
- `cargo clippy -p rutilus-application -p rutilus-persistence -p rutilus-migration -p rutilus -- -D warnings`：通过。
- `cargo test -p rutilus-application center::`：71 通过，0 失败（全仓 365 通过）。
- `cargo test -p rutilus-persistence center_`：44 通过（全仓 228 通过）。
- `cargo test -p rutilus --lib`：153 通过。
- `cargo test -p rutilus-migration`：9 通过，1 失败——`audit_failure_vocabulary::down_refuses_unrepresentable_rows_and_restores_the_000001_shapes` 为并行 agent 对 000003/000004 审计表迁移的在途改动所致，与 A1 无关；`down_order_gate`（5）与 `bare_sql_gate`（13）全部通过。
