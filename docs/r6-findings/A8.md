# R6 对抗循环 · A8 区登记（迁移门禁 / 备份 pin / 数据完整性）

| ID | 严重级 | 修复内容 | 证据 file:line | 测试 |
|---|---|---|---|---|
| R6-D-1 | MEDIUM | down_order_gate 的 `sql_identifier` 漏剥分号：`"DROP TABLE parents; DROP TABLE children"` 形态下标识符变成 `parents;`，与 FK 边永不匹配，父先子的违规被静默跳过。修复：`sql_identifier` 先 trim 空白、再剥引号/括号、再 `trim_end_matches(';')`；`drop_table_names` 对每个空白词按 `;` 切分（兼容 `parents;DROP` 无空格粘连形态），空段丢弃。多语句形态现已可靠归位，无需按门禁自诺（:62-64）整体报失败。 | migration/tests/down_order_gate.rs:268 (`sql_identifier`)、:279 (`drop_table_names`) | 新自检 `gate_checks_multi_statement_raw_drops_separated_by_semicolons`（:1276）：单条 execute_unprepared 内含分号分隔的父先子 DROP 必报违规，子先父必过；连同原有 12 个自检共 13 个全过 |
| R6-D-2 | LOW | backup_snapshot 测试把 `NewerSchema` 的计数硬编码为 28/27，新增迁移会静默失效。改为 `Migrator::migrations().len() + 1` / `.len()` 动态派生，与同文件既有动态断言（`compatibility_accepts_known_schemas_and_rejects_newer_ones` 与 `compatibility_replays_the_wal...`）对齐。 | persistence/src/backup_snapshot.rs:941-944（动态断言块） | `cargo test -p rutilus-persistence backup_snapshot` 全过（含该用例） |
| R6-D-3 | LOW | 000004 up 无预检：携带 target_principal 但 actor 非 `user` 的遗留行（000003 约束集仍放行、产品从未写出的形态）会在拷贝中途被加宽后的 CHECK 以裸约束错误拒绝。修复：up 拷贝前加入与 down（:120-152）对称的预检——统计 `action='change-password' AND target_principal_id IS NOT NULL AND actor <> 'user'`，>0 则具名拒绝（信息含 "000004 up" 与加宽后的 ck_audit_events_target_principal 规则）；模块文档同步修订。 | migration/src/m20260813_000004_audit_operation_vocabulary.rs:158-191（Forward 预检）、:185（拒绝信息） | 新迁移测试 `up_refuses_target_principal_under_non_user_actor_with_migration_context`（tests/audit_operation_vocabulary.rs:389）：system actor 遗留行 → up 具名失败；删除该行后 up 干净应用且保留可表示行 |
| R6-D-5 | LOW | restore 非原子：db 与 WAL 两次 rename 之间的崩溃留下混合对，SQLite 会静默丢弃盐值不匹配的 WAL，恢复的 WAL-only 提交无声丢失。选择"持久化恢复进行中标记 + 预期指纹、下次 open 校验"方案：`restore_database_files` 在首次覆盖前写 `<db>-restore-pending` 侧车（记录快照对与活文件对的 SHA-256 指纹），全部替换成功后删除；`SqliteStore::open` 发现幸存标记时校验活文件对——匹配快照对（恢复已完成）或匹配记录的前状态对（恢复未触及文件）则清除标记放行，其余一律以 `OpenStoreError::RestoreInterrupted` 拒绝。:24 模块文档 "atomically" 表述已改为如实描述（:23-42）。残余：校验只发生在下一次 open（不上电则无校验）；应用层 pre-restore 快照回滚后活文件对与标记记录的前状态逐字节一致，开机会自愈清除标记（app 既有端到端测试 `a_failed_restore_preserves_the_pre_restore_data_for_rollback` 恰走此路径，未破坏）。 | persistence/src/backup_snapshot.rs:208（restore_database_files）、:275（write_restore_marker）、:324（verify_restore_marker）、persistence/src/lib.rs:318（RestoreInterrupted）、:209（open 校验入口） | 新测试三枚：`an_interrupted_restore_is_refused_at_the_next_open`（:730，混合对被拒）、`a_complete_pair_under_the_restore_marker_is_accepted_at_open`（:771，完成对被收并清标记）、`an_untouched_pair_under_the_restore_marker_is_accepted_at_open`（:801，未触及对被收并清标记）；原有 round-trip 与 app 回滚测试全过 |
| R6-D-6 | LOW | dead-letter 构造失败路径（`if let Ok(...)` 的 else）完全静默：吸收记录在无持久化反射下丢失且无任何日志。改为 error 级日志并点名 "durable 反射缺失"，两处吸收分支共用 `log_missing_dead_letter`；`absorption_dead_letter` 的 Errors 文档同步。无界性本身（§9.3 events 表本即 append-only，dead-letter 行降低攻击门槛）登记为残余，不修。 | application/src/center/projection.rs:1410（log_missing_dead_letter）、:866/:878（两处 match 调用点） | 既有 dead-letter 测试（`ownership_absorbed_records_land_durable_dead_letter_rows` 等）全过；构造失败为构建缺陷、实践中不可达，日志路径无直测 |
| R6-D-7 | NOTE | `Center.` 命名空间保留给中心自身的 dead-letter 反射：站点上报该前缀事件会伪造内部管理行、且 dedup 键可能与真实 dead-letter 碰撞。`decode_event` 在 MessageId 解析后拒绝 `Center.` 前缀（拒绝位置在 ingest 层而非 `MessageId::parse`，因 dead-letter 自身经 parse 构造保留 id）。残余：站点侧原始录入（BMC 上报的 MessageId 按 §14.4 展示原始逐字记录）不设前缀拒绝，中心同步接收端才是保留命名空间的守门点。 | application/src/center/projection.rs:1337（decode_event 保留前缀拒绝）、:1323-1351（decode_event 文档） | 新测试 `events_reporting_the_reserved_center_namespace_are_refused`（:3098）：上报 `Center.1.0.EventAbsorbed` 不被存储、游标照常推进 |
| R6-D-8 | NOTE | 词表测试 `in_list_codes` 锚定全 DDL 首个 `redfish_operation IN (`：未来若后续约束再出现同名列表，读取会静默漂移。改为约束名区间内定位：从 `CONSTRAINT ck_audit_events_action` 起、至下一个 `CONSTRAINT` 关键字止的窗口内查找，窗口外任何同名列表都无法偷换读取；约束缺失/列表缺失则响亮失败。 | migration/tests/audit_operation_vocabulary.rs:148（in_list_codes） | `cargo test -p rutilus-migration` 全过（live DDL 与 restored DDL 两侧读取均经新锚定路径） |

## 门禁结果

- `cargo fmt`（三 crate 作用域）通过；全仓 `cargo fmt --all` 因并行 agent 正在编辑的 `web/src/auth.rs` 解析错误中断，非本区引入。
- `cargo clippy -p rutilus-migration -p rutilus-persistence -p rutilus-application -- -D warnings`：在 HEAD + 本区 7 文件改动的工作树（独立 target 目录）上通过。主工作树因并行 security agent 未提交的 `security/src/master_key.rs` 改动（const fn 切片 E0658）无法编译，与本区无关。
- `cargo test -p rutilus-migration`（down_order_gate 13/13，含新自检；audit_operation_vocabulary 全过，含新 up 预检用例）、`cargo test -p rutilus-persistence backup_snapshot`（含 3 枚新标记测试）、`cargo test -p rutilus-application`（projection 相关全过）均在工作树通过。

## 诚实未竟项

1. R6-D-5 校验仅在下次 `SqliteStore::open` 触发；标记文件自身未做目录级 fsync（与既有 replace_file/write_and_sync_file 的文件级同步水准一致）。
2. R6-D-6 dead-letter 无界性未修（§9.3 events 表 append-only 设计使然），仅登记；R6-D-7 站点侧原始录入不设前缀拒绝，仅中心接收端守门。
3. 主工作树当前无法整体编译（并行 agent 的 security/web WIP），本区验证以 HEAD 工作树为基线完成；未 git commit，未触碰 docs/ 下并行登记中的 4 个 .md。
