# 第六轮对抗循环 · 区域 A3 修复登记（web 认证 / 会话 / 限速）

仓库 HEAD：e85560a。本文件由 A3 修复 agent 登记，供评审与后续区域对接。
代码注释为英文，本登记为中文。**未 commit**（按循环纪律由主 agent 统一提交）。

## 修复登记表

| ID | 严重级 | 修复内容 | 证据 file:line | 测试 |
| --- | --- | --- | --- | --- |
| R6-S-1 | MEDIUM | 撤销失败-开放闭合：新增 Web 边界结果 `SessionRevocation::{Revoked, NotFound, AlreadyRevoked}`（`AuthServices::revoke_session` 返回值从 `Result<(), E>` 改为 `Result<SessionRevocation, E>`）；`revoke_session` handler 按边界分类分支——Revoked/AlreadyRevoked→200 成功并审计 ManageSessions（AlreadyRevoked 按幂等成功处理），NotFound→404（不审计），Corrupt/Database/Coordinate 等存储类失败→500 + 仿 B3 纪律记 ManageSessions 失败审计事件（`AuditFailure::SessionRevocationFailed/Inconclusive`）；Standalone 运行时把 `SessionRepositoryError::{NotFound, AlreadyRevoked}` 映射为对应结果、其余错误保持 Err；logout 的撤销失败仍 500 | `web/src/auth.rs:339`（枚举）、`:370`（trait 签名）、`:2617-2660`（handler 分支）、`app/src/standalone_runtime.rs:801-820`（运行时分类映射） | `revoke_session_surfaces_a_storage_failure_as_500_with_audit`（web/src/lib.rs:12817，断言 500 + started/failed 审计对）、`revoke_session_answers_not_found_for_an_unknown_id`（:12877，404 无审计）、`revoke_session_treats_a_repeat_revocation_as_idempotent`（:12928，重复撤销 200 + 成功审计） |
| R6-S-2 | LOW | 双重 Set-Cookie：中间件在 append 会话 cookie 前检查响应已含 `Set-Cookie`（logout / change-password 已 insert 清除头 `Max-Age=0`）——不再在清除头之上追加续期头（浏览器会同时收到清除与续期两个头，续期胜出使退出失效） | `web/src/auth.rs:1494-1508`（append 前 `get(SET_COOKIE).is_none()` 守卫） | `logout_and_password_change_carry_exactly_one_set_cookie`（web/src/lib.rs:12989，`get_all(SET_COOKIE).count()==1` 且为清除头） |
| R6-S-8 | LOW | bootstrap 未知主体分支审计缺失：`bootstrap_complete` 在主体查找失败（admin 主键不存在）分支补 `record_login_failure(&state, None, now)`（仿 login 未知用户名分支，无主体可指认），不再静默 401 | `web/src/auth.rs:2136-2146` | 既有 bootstrap 审计路径回归（该分支与 login 未知用户共用 `record_login_failure` 形状；worker/route 套件全绿） |
| R6-S-9 = R6-W-5 | LOW | 用户名限速键归一化：`bounded_username_key` 从"原样截断"改为"归一化投影"——trim + to_lowercase 后截断到 `RATE_LIMIT_USERNAME_CHARS`，保持"无效名也消耗预算"语义（截断只合并不合法名，不收紧合法主键预算）；已是规范投影且在界内的值走零分配 Borrowed 路径 | `web/src/auth.rs:1404-1427`（投影实现）、`:124-134`（常量注释） | `rate_limiter_username_key_normalizes_case_and_whitespace`（web/src/auth.rs:4609，`Admin`/` admin `/`ADMIN` 共享同一桶、跨地址同桶、投影断言） |
| R6-S-10 | LOW | admin 404 分支无限速：`set_user_state` 与 `assign_user_role` 在查找前以（actor 主键 + IP）经 `password_change_limiter.reserve`（不 refund，与 `set_user_password` 同构）——未知主键探测连续触发 404 后限速 429，探测不能免费跑 dummy 派生门 | `web/src/auth.rs:2770-2810`（set_user_state 预留）、`:2875-2915`（assign_user_role 预留） | `admin_state_and_role_404_floods_are_rate_limited`（web/src/lib.rs:13046，5×404 后 state 请求 429，role 共享同一预算同样 429） |
| R6-S-13 | NOTE | bootstrap_pending 失败-开放闭合：`me` 的 `has_unconsumed_bootstrap_code()` 从 `unwrap_or(false)` 改为 `unwrap_or_else(true + warn)`——首次运行闸读失败必须倒向"claim 待定"，绝不倒向"产品已开放" | `web/src/auth.rs:2509-2519` | `me_answers_claim_pending_when_the_bootstrap_read_fails`（web/src/auth.rs:5638，worker 双件 `bootstrap_read_ok=false` 时 me 应答 `bootstrap_pending=true`） |
| R6-W-4 | LOW | IPv4-mapped 双桶：`request_ip` 归一化——v6 地址先 `to_ipv4_mapped()`，映射得回 v4 则取 v4 串，纯 v6 保留自身串（双栈内核以 `::ffff:x.x.x.x` 呈现 v4 对端，同一客户端不再分两个限速桶） | `web/src/auth.rs:1682-1698` | `request_ip_normalizes_ipv4_mapped_addresses`（web/src/auth.rs:4137，映射/纯 v6/纯 v4/未知回退四态断言） |
| R6-W-9 | LOW | `me` 无限速：Public 的 `/api/v1/auth/me` 挂轻量 per-IP 查询限速——`AuthState::me_limiter`（独立 LoginRateLimiter 实例，仅用 IP 桶、无用户名维度、无 refund，查询频率即账目），窗口内 `ME_IP_QUERY_LIMIT`(=60，页面加载一次查询，上限高于登录失败预算) 后 429 | `web/src/auth.rs:143-147`（常量）、`:1041-1056`（me_limiter）、`:1226-1233`（reserve_ip）、`:2488-2519`（handler） | `me_answers_429_under_a_per_address_query_flood`（web/src/lib.rs:13106，61 次查询后 429） |
| R6-C-5 | PARTIAL | refund 精确化：桶条目从 `(Instant, String)` 改为 `(Instant, String, u64)`，每桶 `next_token` 计数器逐条铸 token；`reserve` 返回 `Reservation{username_token, ip_token}`，`refund` 按 token 精确删除本请求条目（补偿路径同样按 token 归还）——同地址并发交错下不再可能"身份互换"（旧"最新同地址弹"在两条同地址在途请求交错时会把失败条目的时间戳换成早先条目）；原 :1241-1247 注释改为诚实表述：旧弹法仅影响到期时序、不影响计数裁决，token 键控后归因精确 | `web/src/auth.rs:1134-1137`（Reservation）、`:1146-1190`（reserve/补偿/sweep）、`:1192-1216`（refund 文档与实现）、`:1239-1364`（reserve_*_key 铸 token、refund_key_by_token） | `rate_limiter_refund_removes_exactly_the_presenting_address_slot`（web/src/auth.rs:4292，扩展断言幸存条目标识 == 保留请求自身 token）、`rate_limiter_refund_conserves_the_count_under_concurrent_interleaving`（:4427，6 线程并发成功+失败交错：桶长 == 保留数且每条目 token ∈ 保留集合）、`rate_limiter_refund_keeps_each_entries_own_expiry_timing`（:4507，到期时序按条目自身时间，窗口边界不提前放行） |

## 门禁执行情况

`cargo fmt`（工作区，--check 干净）/ `cargo clippy -p rutilus-web -- -D warnings`（0 错误；期间顺手修掉并行 agent 在 `application/src/center/dispatch.rs`、`application/src/center_sync.rs` 遗留的 3 个机械 clippy 违例以解锁门禁）/ `cargo test -p rutilus-web --lib auth::`（44 通过）/ `cargo test -p rutilus-web`（118 lib + 65 集成共 183 通过）；`cargo build -p rutilus` 通过（`SessionRevocation` 运行时映射随 app 编译验证）。未竟项：`cargo test -p rutilus --lib` 因并行 agent 在 `app/src/center_runtime.rs:1582` 的在途测试代码（`CenterOperationRefusal` 未实现 Error）失败，非本区域代码；`cargo test -p rutilus-persistence` 未执行（本区域未改 persistence，但其 revoke_session 错误变体为本修复的分类依据）。
