# 第六轮对抗循环 · A5 修复登记（CI 工作流 / 门禁脚本 / 依赖 deny）

HEAD = e85560a，修复日期 2026-08-14。核心文件 `.github/workflows/ci.yml`、`scripts/assert-tests-ran.sh`、`deny.toml`。**已 commit**（2026-08-14，主 agent 统一提交为 `fcf7257`）。

| ID | 严重级 | 修复内容 | 证据 file:line | 测试 |
|---|---|---|---|---|
| R6-W-1 | MEDIUM | wasm freshness 门禁 triple 归一化实现：原 ci.yml:342-344 自述"下一次 ubuntu 运行必红、triple 归一化计划下一迭代"——现以目标化 python3 变换（对两侧比较副本施加同一替换）实现 `1.97.1-<host-triple> -> 1.97.1-toolchain`，并覆盖 `.cargo-home` 与 rustup 路径类；注释同步改写（不再声称"计划中/必红"，如实描述"归一化仅覆盖已登记路径类"） | `.github/workflows/ci.yml`（Build wasm32 UI artifact 步骤注释 + `python3 - <<'PYEOF'` 变换，含 `re.sub(rb'1\.97\.1-[^/]*', b'1.97.1-toolchain', seg)`） | 本机推演级验证：对已提交 `web/assets/rutilus_ui_bg.wasm` 实测内嵌 Windows triple 字符串形态（`strings` + NUL 段分析：21 个含 `1.97.1-` 的段全部为 `.cargo-home\.rustup\toolchains\1.97.1-x86_64-pc-windows-msvc\...`）；构造合成 ubuntu 侧副本（同名段转 `/` + linux triple）后两侧经同一变换**字节一致**（`committed-norm == ubuntu-norm: True`）。**ubuntu 侧真实收敛待下一次 CI 运行验证**（已如实标注于 ci.yml 注释与本节末"未竟项"） |
| R6-W-2 | MEDIUM | 整文件 `tr '\\' '/'` 掩蔽 0x5C↔0x2F 字节类：改为**目标化归一**——仅对已登记的两类路径字符串段（`.cargo-home` 段、`ui\src`/`ui/src` 前缀段）做 `\`→`/` 替换；代码段 LEB128 立即数等其余 0x5C 字节不再被触碰。掩蔽范围如实量化登记：1368 个 0x5C 字节中 422 个位于登记段（62 个 `.cargo-home` 段含 418 + 2 个 `ui\src` 段含 4，共 276994 个 NUL 段），剩余 946 个（含 816 个代码段 LEB128）不被触碰；原 tr 触碰全部 1368 | `.github/workflows/ci.yml`（"Scope of the mask, stated honestly (R6-W-2)" 注释段；变换代码 `if b'.cargo-home' in seg or seg.startswith(b'ui\\src') or seg.startswith(b'ui/src')`） | 本机实测：变换后副本 0x5C 余量 = 946（= 1368 − 422，与登记一致）；变换前后仅登记段内字节改变；合成 ubuntu 侧收敛测试同时覆盖本项 |
| R6-W-7 | LOW | 测试数 floor 可被掏空：`scripts/assert-tests-ran.sh` 新增 `--expect-tests name1,name2,...`——解析 `cargo test` 输出的 `test <name> ... ok` 行，断言关键测试名存在且运行（不只计数）；名字校验 `[A-Za-z0-9_]+`，单元测试模块前缀（`module::tests::foo`）按裸名后缀匹配；`#[ignore]` 的测试（`... ignored`）视为未运行。ci.yml secret gate（pin 10）传入 4 个 workspace-scan 测试名，migration（pin 50）传入 down_order_gate 6 个核心测试名 | `scripts/assert-tests-ran.sh`（`--expect-tests` 解析循环与名字断言块）；ci.yml Secret leak gate / Migration test 步骤 run 行 | cargo 桩（stub）单元测试 4 例全过：T1 计数+名字全中 exit 0、T2 缺名 exit 1、T3 计数低于 pin exit 1、T4 非法名 exit 1；21 个登记名全部在 `cargo test --workspace -- --list` 实测输出中核在（每名 1 处）；`bash scripts/assert-tests-ran.sh --help` 用法冒烟 exit 1（用法错误路径） |
| R6-W-8 | LOW | 无 ran-断言步骤：Capability ledger / Release baseline / workspace Test 三步全部改走 `assert-tests-ran.sh <pin>`——pin 为本机 `--list` 实测（capability ledger 295、release baseline 14、workspace 1913，均在 HEAD e85560a 复测且与 release-readiness.md §五 门禁复跑 2026-08-14 口径一致），并各附关键测试名清单（ledger 2、baseline 6、workspace 6 跨 6 crate）；"Ran-assertion coverage" 注释块更新为五步全覆盖 | `.github/workflows/ci.yml`（Test (full workspace) / Capability ledger check / Release baseline check 步骤 run 行 + 注释） | 本机实测计数：`cargo test --locked -p rutilus-infra-redfish -- --list` = 295、`... release_baseline -- --list` = 14、`cargo test --locked --workspace -- --list` = 1913（含 1 doc-test，与文档 1912+1=1913 吻合）；全量 run 侧验证受阻于并行 agent 进行中的编译错误（见未竟项） |
| R6-8 | NOTE | deny unmatched-skip 无触发器：cargo-deny 步骤加 `command-arguments: --deny warnings`（0.20.2 无 `--deny-warnings` 旗标，已核实；`arguments` 输入落在 `check` 之前故用 `command-arguments`）；8 条不可达 skip 条目核实后从 deny.toml 删除（ahash@0.7.8/0.8.12、getrandom@0.3.4、hashbrown@0.12.3、rand@0.10.2、r-efi@5.3.0/6.0.0、sha2@0.11.0）。"改写 reason 并保留"二选一**不可行**：cargo-deny 官方 bans 配置文档确认无 per-entry unmatched-skip 容忍，保留即 warning 恒在、`--deny warnings` 下必红——故唯一可行路径是删除，且删除后"可达即需复核"语义由 multiple-versions 检查自然承接（已登记 deny.toml 注释） | `deny.toml`（skip 列表删 8 条 + R6-8 注释）；ci.yml Deny checks 步骤 | 本机实测：`cargo deny check` 恰报 8 条 unmatched-skip（与登记一致）；删后 `cargo deny --all-features check --deny warnings` → "advisories ok, bans ok, licenses ok, sources ok" exit 0；触发器验证：临时插回 1 条 unmatched skip 后同命令 exit 2；`cargo tree --workspace --target all --all-features -i <crate>@<ver>` 核实 7 条完全不在解析图、r-efi@6.0.0 仅在 `cfg(target_os = "uefi")`（getrandom 0.4.3→uuid→leptos 链）下可达、不在 [graph].targets |

## 门禁验证手段

- **cargo fmt --check**：本修复不涉及 Rust 源码，不适用（无 Rust 文件改动）。
- **ci.yml YAML 合法性**：本机无 yq；改用 PyYAML `yaml.safe_load` 校验通过（jobs: ci + release-artifacts，23 个步骤齐全）。heredoc 缩进陷阱已处理：python 块全部按块标量缩进（YAML 剥公共缩进后 `PYEOF` 落在列 0，bash 方可识别定界符）；另将 wasm 步骤 run 块整体抽出 `bash -n` 语法检查通过，并实际执行提取出的 heredoc 得到与本地验证副本**字节一致**的输出。
- **`bash scripts/assert-tests-ran.sh --help` 冒烟**：用法错误路径 exit 1（与既有行为一致）。
- **cargo deny check 实测**：`cargo deny --all-features check --deny warnings`（与 CI 动作实际拼装命令一致，动作 Dockerfile 锁 0.20.2）exit 0 全绿；触发器负向实测 exit 2。

## 诚实未竟项

1. **ubuntu 侧 wasm 收敛待下一次 CI 实跑**（R6-W-1/W-2 的核心验证无法本机完成）：本机只能推演——已提交 wasm 内嵌 Windows triple 形态实测确认、合成 ubuntu 侧副本收敛字节一致；但 ubuntu 构建产物在真实 runner 上的实际字节（rustup/cargo 均落 `$HOME` 的推断依据 GitHub runner 镜像 install-rust.sh 源码：运行时环境不导出 RUSTUP_HOME/CARGO_HOME，仅 PATH 追加 `$HOME/.cargo/bin`）需下一次 ubuntu CI 运行证实。若真实形状偏离登记类（如相对路径带 `./` 前缀），门禁会红并携带诊断上传，届时按诊断扩展登记类即可。
2. **workspace / migration 全量 run 侧本机复测受阻**：轮次中途并行 agent 正在修改 `migration/tests/audit_operation_vocabulary.rs`（`Entity::delete_all()` 编译错误）、`security/tests/secret_leak_gate.rs`（2 测试在改中失败）等，`cargo test --locked --workspace` 与 `cargo test --locked -p rutilus-migration` 本机无法跑绿。pin 值（1913 / 50 / 57 口径）依据 HEAD e85560a 的 master 实测（release-readiness.md §五 门禁复跑 2026-08-14）+ 本机 `--list` 复测；脚本逻辑已用 cargo 桩全量验证。并行 agent 收尾后若计数与 pin 不符需复核。
3. **yq 本机不可用**：ci.yml 校验改用 PyYAML（见上），未跑 yq。
4. **登记段内差异对门禁不可见**（R6-W-2 残余，已写入 ci.yml 注释）：新增依赖的 panic-location 字符串若位于 `.cargo-home/.cargo/registry/src/...` 段内，其差异被掩蔽；该残余为登记类的定义内残余，非实现缺陷。
