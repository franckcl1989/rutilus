# 第六轮对抗循环 · 区域 A6 修复登记（web 错误映射 / API 契约 / UI / wasm 产物）

仓库 HEAD：fcf7257。本文件由 A6 修复 agent 登记，供评审与主 agent 收口。
代码注释为英文，本登记为中文。**已 commit**（2026-08-14，主 agent 统一提交为 `7c6ac9d`，11 文件 +841/-101）。

## 修复登记表

| ID | 严重级 | 修复内容 | 证据 file:line | 测试 |
| --- | --- | --- | --- | --- |
| R6-W-3 | MEDIUM | 伪造 source="center"：`create_operation` 服务端 pin——HTTP 控制台面只接受 `standalone`/`site`（Site 姿态提交 `site`、Standalone 姿态提交 `standalone`，缺省 `standalone`），`center` 是中心 offer 流（application dispatch）保留源，wire 串一律 400 拒绝且不落库；`project_operation_source` 只读侧不变（中心派发操作仍如实投影 `center`） | web/src/lib.rs:2312-2323（pin 分支，注释 R6-W-3）；web/src/lib.rs:2923-2927（读侧投影不变） | web/tests/operation_path.rs:1168 `accepts_an_explicit_operation_source`（反向测试：standalone/site → 201 且落库 source 一致；`source: "center"` → 400、消息点名 center、仓库仅 2 条且无 Center 源） |
| R6-W-6 | MEDIUM | 制品无大小上限：`ArtifactStore::create` 新增服务端总大小上限 `ARTIFACT_MAX_SIZE_BYTES = 2 GiB`（选值理由：服务器 BMC 固件实际至多数百 MiB，2 GiB 留一个数量级余量同时把单制品的磁盘/在途内存占用封顶；cap 是 create 时的**声明总大小**约束——每块 4 MiB wire 上限管单请求，`ChunkExceedsSize` 管累计不超声明，两层合起来累计永不超 cap）；`size_bytes == 0`（既有 `ZeroSize`）与超限（新 `SizeExceedsLimit { size_bytes, maximum }`）都拒绝；web 层 create 映射 400（chunk/finalize 的"不可能产生"兜底组同步补该变体） | application/src/artifact_store.rs:66-76（常量与理由）、:242-252（create 拒绝）、:668-669（新错误变体）；web/src/lib.rs:2565-2568（create 400 映射）、:2691-2700、:2755-2766（兜底组） | application/src/artifact_store.rs:1023 `create_refuses_a_declared_size_above_the_artifact_cap`（cap+1 → `SizeExceedsLimit` 且零落库；恰在 cap → 接受）；:1068 `chunks_at_the_cap_boundary_stay_bounded_by_the_declared_size`（cap 边界声明 + 首块落盘；小声明跨声明尾 → `ChunkExceedsSize`、received 停在 3） |
| R6-E-11 | MEDIUM | 审计历史不可达：web `GET /api/v1/audit` 解析 `offset`（非负整数、缺省 0、上限 `AUDIT_QUERY_MAX_OFFSET = 10_000_000` 沿用 `AUDIT_QUERY_MAX_LIMIT` 风格，重复参数拒绝）；`offset == 0` 走 `list_recent_events`（内存尾），`offset > 0` 走 `list_recent_events_with_offset`（store 有界列表，fail-closed 503 不变）；响应新增 `has_more`（返回条数 == limit 时 true）；api 契约 `AuditQueryResponse` 加 `has_more` 字段——响应方向按 W3C-1 去 `deny_unknown_fields`（旧 console 容忍新键），字段 `#[serde(default)]`（新 console 可解析旧 server 无该键的响应）；UI AuditView 新增「加载更早」按钮（offset 递增 limit，仅 `has_more` 时显示），分页失败保留已载窗口，`hint_audit` 如实披露窗口语义 | web/src/lib.rs:2141-2171（handler）、:1693-1698（`AUDIT_QUERY_MAX_OFFSET`）、:3422-3478（`parse_audit_query`/值解析）；api/src/lib.rs:2831-2871（信封字段与注释）；ui/src/lib.rs:4452-4524（`AuditListState`/`AuditPage`/`with_older`）、:14901-14914（加载更早闭包）、:14960-14976（按钮）、:15807-15825（`AUDIT_QUERY_LIMIT` 与 `fetch_audit(offset)`）；ui/src/i18n.rs:233（`action_load_earlier`）、:1635-1636（`hint_audit` 双语更新） | web/tests/write_path.rs:1371 `bounds_the_audit_query_limit`（新增 `?offset=abc`/`?offset=-1`/`?offset=10000001`/重复 limit/重复 offset 拒绝；`has_more=false` 断言）；:1421 `pages_the_audit_history_with_an_offset`（5 事件 limit=2：offset 0 → 最新 2 条 has_more=true；offset 2 → 次 2 条；offset 4 → 短页 has_more=false；offset 6 → 空页）；api/src/lib.rs:10711-10722（`has_more` 往返 + 无键 payload 解析为 false + 未知键容忍）；ui/src/lib.rs:21611 `audit_paging_appends_older_pages_and_tracks_the_next_offset`（追加顺序、offset 步进、has_more 跟随） |
| R6-A1 接线 | MEDIUM | A1 新变体接线：`CenterOperationRefusal` 新增 `SiteBindingRevoked` 与 `UnknownOutcomePending { operation_id }`；`CenterOperationRefusalCode` 新增稳定码 `site_binding_revoked`/`unknown_outcome_pending`（snake_case 自动产出，`serde(other) Unknown` 兜底不变，旧 console 无线兼容问题）；`center_dispatch_refusal_response` 新分支——两个变体都映射 **409 Conflict**，理由：refusal 家族里 403 保留给 actor 授权判定（`NotAuthorized`/handler 作用域闸门）、422 保留给 body 引用未知；吊销绑定与未决 unknown 结局既不是授权失败也不是引用未知，而是被寻址站点当前状态与派发冲突（绑定需重建、同 key 已有未决操作），409 是家族语义一致的"判定而非授权"状态；消息点名站点绑定或既有 operation id；`center_runtime.rs` 两个 `TODO(R6-A6)` 临时映射替换为类型化变体 | api/src/lib.rs:6109-6113（新稳定码）；web/src/lib.rs:621-624（新变体）、:4269-4287（409 状态理由注释）、:4315-4327（新分支与消息）；app/src/center_runtime.rs:500-507（`SiteBindingRevoked`）、:541-548（`UnknownOutcomePending`） | web/src/lib.rs:13743 `the_site_binding_and_unknown_outcome_refusals_map_to_conflict_with_stable_codes`（两变体各 409 + 码断言 `site_binding_revoked`/`unknown_outcome_pending` + 消息点名）；app/src/center_runtime.rs:1605 `dispatch_to_a_revoked_site_is_refused_and_the_revocation_ends_the_session` 改为断言 `SiteBindingRevoked` |

## wasm 产物再生

- UI 改动后按 ci.yml 流程再生：`--remap-path-prefix=C:\Users\franck=.cargo-home --remap-path-prefix=C:\Users\franck\Documents\rutilus=.` 下 `cargo build --locked -p rutilus-ui --target wasm32-unknown-unknown --release`（rust-toolchain.toml 1.97.1 + rust-src），wasm-bindgen-cli 0.2.126（与工作区 wasm-bindgen 版本一致，复用 target/rutilus-tools 既有安装）`--target web --no-typescript --out-name rutilus_ui` 输出后覆盖 `web/assets/` 的 `rutilus_ui.js` 与 `rutilus_ui_bg.wasm`。
- **如实注记**：首次构建实际失败（Leptos 0.8 `#[component]` 宏展开作用域下 crate 根私有 struct `AuditPage` 不可见，E0433——后台任务管道 `| tail` 掩盖了非零退出码，产物未更新）；在 `AuditView` 组件体内加 `use crate::AuditPage;` 后构建成功，`strings` 验证新 UI 文案（`Load earlier`、`older history loads in pages`）确实进入 wasm，与既有 zh 文案同样不入 wasm 的既定形态一致。
- 本地对照：生成产物仅含已登记路径类（`.cargo-home` 前缀 62 段、`ui\src` 相对段、`1.97.1-x86_64-pc-windows-msvc` triple），`grep` 验证无任何 `C:\`/盘符绝对路径泄漏；与上一版提交产物的路径类 profile 逐段一致；wasm freshness 门禁的 CI 侧比较（同一 remap + 同一工具链 + 同一生成机）前提已满足，最终判定以 CI 实跑为准。

## 门禁结果

- `cargo fmt --all --check`：通过。
- `cargo clippy --workspace --all-targets -- -D warnings`：通过（全仓零警告；`with_older` 按 needless_pass_by_value 取引用）。
- `cargo test -p rutilus-web`：11 个测试二进制全绿（lib 119 + 各集成面）。
- `cargo test -p rutilus`：153 + 16 + 2 + 3 通过。
- `cargo test -p rutilus-ui`（host）：142 通过。
- `cargo test -p rutilus-application`：367 + 4 通过（artifact_store 23 项含 2 个新测试）。
- `cargo test -p rutilus-api`：85 通过。
- **未 commit**（循环纪律）。

## 诚实未竟项

- **wasm freshness 门禁以 CI 实跑为最终判定**：本机即原产物生成机（remap、工具链 1.97.1 + rust-src、wasm-bindgen 0.2.126 与 CI 一致，路径类 profile 与上一版提交产物逐段一致），本地对照已通过可执行的全部检查（无盘符泄漏、仅登记路径类、新文案入 wasm）；CI 侧逐字节比较仍需 CI 实跑确认，若有差异以 CI 上传的诊断产物 diff 后由主 agent 决定。
- **Center 姿态的审计分页仅经同一 handler 共享**：`GET /api/v1/audit` 在 Edge 与 Center 两个路由面共用 `audit_query` handler，offset 语义自动覆盖；未为 Center 姿态单独增加分页集成测试（Center 测试台 mock 的审计查询返回 Err，503 断言已覆盖）。
- **`AuditEventResponse`（事件级）仍保留 `deny_unknown_fields`**：本轮只放宽了信封 `AuditQueryResponse`（W3C-1 方向）——事件级严格性有既有测试 `audit_contract_is_secret_free_and_strict` 明确钉死（"secret_free_and_strict"），且本轮未向事件级加字段，故未触碰；若未来向事件级加字段，需先按 W3C-1 放宽。
