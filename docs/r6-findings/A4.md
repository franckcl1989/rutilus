# 第六轮对抗循环 · 区域 A4 修复登记（secret-leak 机械门禁 / master-key KDF）

仓库 HEAD：e85560a。本文件由 A4 修复 agent 登记，供评审与后续区域对接。
代码注释为英文，本登记为中文。**已 commit**（2026-08-14，主 agent 统一提交为 `fcf7257`）。
登记时并行 agent 正在修改 docs/ 下 4 个 .md 与若干 crate 源文件（application/、migration/、web/、persistence/ 等），本区域未触碰这些文件。

## 修复登记表

| ID | 严重级 | 修复内容 | 证据 file:line | 测试 |
| --- | --- | --- | --- | --- |
| R6-S-3 | MEDIUM | 标识符集缺口：`SENSITIVE_IDENTIFIERS` 加入 `binding_code`；`SENSITIVE_IDENTIFIER_SUFFIXES` 无条件加入 `_pwd/_passwd/_pw/_passcode`；`_key/_pin` 按误报张力单独登记（`TENSION_IDENTIFIER_SUFFIXES`）——仅绑定非空字面量时触发 [R1]，不进入 [R3] 日志集、非字面量绑定不触发；头文档 :38-43 段落按 "registered false-positive" 纪律登记张力；因 `_key` 后缀新增命中的 5 个生产常量（`ENTRY_TLS_PRIVATE_KEY`、`CENTER_KEY_FILE`、`CENTER_CA_KEY_FILE`、`SITE_CLIENT_KEY_FILE`、`DELTA_OEM_KEY`）登记进 `ALLOWED_CONSTANT_HITS`（2→7 条，path+line+name+value 绑定，漂移即红） | `security/tests/secret_leak_gate.rs:517`（标识符集）、`:541`（无条件后缀）、`:565-578`（`_key/_pin` 张力）、`:601`（ALLOWED 扩表）、头文档 `:52-73` | `binding_code_and_new_compound_suffixes_are_flagged`（gate:1853，7 报 7 过含 `DEFAULT_TOTP_KEY` 形态）；全仓 workspace 扫描保持全绿 |
| R6-S-4 | LOW | PEM 转义击穿：`parse_plain_string`（:246-266 原实现只透传反斜杠）改为完整转义解码——`\\ \' \" \n \r \t \0 \xNN \u{...}` 及反斜杠换行续行，content 与编译值一致（byte 字符串同路径自动覆盖）；解码失败/非法形态透传不崩溃；头文档 :17-19 声明更新（"转义解码后的内容"）；[R1] 违规消息与 `ALLOWED_CONSTANT_HITS` 比对改用解码后文本（现有条目无转义，无回归） | `security/tests/secret_leak_gate.rs:339`（`parse_plain_string`）、`:306`/`:316`（`\xNN`/`\u{...}` 解码辅助）、头文档 `:16-24` | `escaped_pem_literals_are_flagged_after_escape_decoding`（gate:2025）：`\u{50}` 与 `\x2D` 全块变体（含 byte 字符串）必须报 [R2]，仅头无尾必须放行 |
| R6-S-5 | LOW | `OUTPUT_MACROS` 缺 `print!`/`eprint!`：加入（3→5），`print!`/`eprint!` 与 `println!`/`eprintln!` 同等待遇；头文档 :95-99（[R3] 宏清单）与 :133-137（排除段，明确 print!/eprint! 是输出面）同步登记 | `security/tests/secret_leak_gate.rs:654`、头文档 `:124-126`、`:157-162` | `print_and_eprint_macros_are_output_surfaces`（gate:2221，4 报 3 过） |
| R6-S-6 | LOW | 头文档 :34-36 显式登记盲区：const/static 链（`const S: &str = "..."; let password = S;`——常量子身仅当常量名敏感时被报）与方法调用形态（`let password = config.secret();`）不解析——如实、不含糊，token 扫描刻意不用语义知识 | 头文档 `:43-50` | 无新测试（文档登记项）；既有 passing 集回归 |
| R6-S-7 | LOW | concat! 片段规则从 name-gate 改为跨片段三元组判定：`concat!/format!` 参数片中同时存在含 `BEGIN` 片段、含 `END` 片段、含 `PRIVATE KEY` 片段 → 无论绑定名是否敏感一律报 [R2]；writer 形态（仅 BEGIN 侧，无 END/PRIVATE KEY）与 BEGIN+PRIVATE KEY 无 END 对保持放行；头文档 :65-74 理由表述重写（登记 prose 三元组误报边） | `security/tests/secret_leak_gate.rs:1132`（`pem_fragment_violation`）、`scan_file` 接线（非敏感名也检查，约 `:1460-1475`）、头文档 `:95-107`、`:116-127` | `concat_format_fragments_with_pem_material_are_flagged`（gate:2122）：`let pem = concat!("-----BEGIN PRIVATE KEY-----", body, "-----END PRIVATE KEY-----");` 必须报；原 name-gate 误报例（`api_key = concat!("-----BEGIN ", "PRIVATE KEY-----")` 等）移入 passing |
| R6-S-11 | NOTE | master-key KDF 对齐产品基线：`derive_wrapping_key` 改用 `rutilus_domain::ARGON2ID_MEMORY_KIB/TIME_COST/PARALLELISM`（64 MiB / 3 趟 / 1 并行，与口令派生同源防漂移）；信封魔数升版 `RUTMK001→RUTMK002`（`ENVELOPE_MAGIC`），旧版 `LEGACY_ENVELOPE_MAGIC` 在 `from_bytes` 仍接受、`is_legacy()` 可查、`recover_master_key` 按信封自带版本选择派生参数与 AAD 魔数解锁；解锁路径（`initialization_runtime` resumed 与 `StandaloneInstance::open`）在解锁成功后经 `rewrap_master_key`（Passphrase 目标）迁移重保护并落盘（新增 `MasterKeyFile::replace` 原子替换，runtime 锁内执行）；`rewrap_master_key` 文档注明 Passphrase 重包即迁移；backup_package 注释同步 RUTMK002 | `security/src/master_key.rs:16`/`:21`（魔数）、`:134`/`:151`（接受+查询）、`:447`/`:474`（两代派生）、`:497`（AAD 带魔数）、解锁分发 `:225-250`；`platform/src/master_key_file.rs:97`（replace）；`app/src/standalone_runtime.rs:2792`（迁移辅助）、`:1844`（open 接线）、`:2815`（错误枚举）；`app/src/initialization_runtime.rs:139`（resumed 接线）；`security/src/backup_package.rs:37`/`:65` | `legacy_v1_envelope_unlocks_and_rewraps_to_the_current_format`（master_key.rs:965）：旧版信封（测试内按 v1 原样构造：默认参数+旧魔数+旧 AAD）解锁成功、错口令仍 AuthenticationFailed、rewrap 后为新魔数且可解锁；`replace_rewrites_the_existing_key_file_with_the_migrated_envelope`（platform master_key_file.rs:315）；既有 44 项 security 单测、7 项 platform 单测、15 项门禁自测全绿 |
| R6-A4-B1 | LOW（附加） | 门禁 tokenizer 两个机械缺陷（排查 S-3 登记线号漂移时发现，超出指派清单）：① byte 字符字面量 `b'-'`/`b'_'` 未被识别——`b` 被扫成标识符、`'_` 被误判为 lifetime、残留 `'` 的字符扫描一路吞到下一个撇号（redfish_gateway.rs:7435 的 `b'_'` 曾吞掉 3152 字符含注释，并把注释文本泄漏进 token 流）；② 行号计数器不补偿多行 raw 字符串与 `\<newline>` 续行内的真实换行，导致其后所有报点行号系统性偏低（`DELTA_OEM_KEY` 报 13404 实为 13476）。修复：`b'...'` 分支整字面量消费 + `skip_char_or_lifetime` 的 `'_` lifetime 判定加"后随非 `'`"守卫（`'_'` 是字符字面量不是 lifetime）+ 字符串/raw 字符串解析返回消费的真实换行数、主循环补计。行号修复后 `ALLOWED_CONSTANT_HITS` 的 DELTA_OEM_KEY 条目以真实行 13476 匹配 | `security/tests/secret_leak_gate.rs:497`（`skip_char_or_lifetime`）、tokenize 的 `b` 分支 `:273-296`、raw/plain 解析换行计数 `:339` 起、头文档 `:16-24` | `byte_char_literals_do_not_derail_the_tokenizer`（gate:2085，4 例）；`multi_line_literals_do_not_drift_reported_lines`（gate:2066）；workspace 扫描报点行号恢复真实源行 |

## 诚实未竟项 / 交接说明

1. **app 测试与全量 clippy 受阻**：`cargo test -p rutilus`（app 名 `rutilus`）的测试二进制当前被并行 agent 的在途代码阻断——`standalone_runtime.rs:3803` 附近新加 sea_orm 裸连接测试与 `center_runtime.rs:1582` 对 `CenterOperationRefusal: Error` 的依赖与当前 lockfile/API 不匹配（本区域未触碰这些代码）。已验证：`cargo check -p rutilus`（lib）通过、`cargo clippy -p rutilus --no-deps --lib -- -D warnings` 通过（含本区域全部接线）。迁移行为在 security 层测试覆盖；app 解锁路径的迁移接线待并行 agent 代码收敛后由 `cargo test -p rutilus` 回归。
2. **`b'-'` 同类缺陷的孪生文件**：头文档声称与 `migration/tests/bare_sql_gate.rs` 共用同一 tokenizer；该文件的 tokenizer 可能仍有同类 byte 字符字面量缺陷。migration/ 目录由并行 agent 在改，本区域未触碰——建议后续区域核对。
3. **web/src/auth.rs 与 docs/ 4 个 .md** 属并行 agent，本区域零触碰；`cargo fmt --all` 曾误触其他在途文件（格式化收敛，无语义改动），后续区域建议只对自有文件跑 rustfmt。
4. R6-S-12（run_standalone 删除）由 A2 登记（见 A2.md），本区域未处理。
